# BACKLOG — ai-council

## Big picture

`ai-council` is the ecosystem's multi-model deliberation engine: it runs structured
debate/research across a configurable AI panel and produces the verdicts that become
binding ADRs. The backlog advances the tool toward a delegation-ready, evidence-based,
reliable, and self-enforcing state across seven themes.

**Themes (backbone) — epic ids:** [E1] Invocation surface & delegation-readiness · [E2] Synthesizer refresh ·
[E3] Provider reliability & CLI engine · [E4] Model currency · [E5] Naming & quality automation ·
[E6] Council process & epistemic quality · [E7] Record & governance hygiene

---

## [E1] Invocation surface & delegation-readiness
> As the tool owner, I want ai-council's invocation specs and outputs to live in a clean,
> delegation-ready surface, so an external agent/operator can commission the Council without
> ambiguity about where specs live or where results land.

_Prior stories delivered as of 2026-07-02 (#12–#15); see JOURNAL 2026-07-02 and git. Theme backbone retained per the story-map (themes are the stable backbone; delivered stories/tasks leave per ADR-65)._

### [S10] Deliver the delegation-ready caller surface
So that an external caller gets a contract-honest Lane A and a transcript-free verdict artifact.
- [#34] [P3][M] Research-path verdict-package parity (R6 lane-parity): `run_research` (`research/output.py`) emits no `council-verdict-*.json` — only the debate/orchestrator path does. A Lane A research commission therefore gets no transcript-free deliverable · Done when: a research run emits a DRAFT-INT-1 verdict package in every destination, mirroring the debate path · refs L-INT R6, DRAFT-INT-1, the inbox-mirror blind-spot memory; #26 shipped debate-path only per architect ruling 2026-07-17
- [#75] [P2][S] **`secondary_dir` raises where `target_paths` swallows — the same canonical-loss class this contract exists to remove** (Lane A1 escalation, 2026-07-19 integration; PRE-EXISTING, not introduced by A1): `_write_routed` treats a `secondary_dir` write as required-and-raising while ADR-43 `target_paths` mirrors stay best-effort, so a `secondary_dir` failure **aborts before the minority report and verdict package get their CANONICAL copies** — the exact "a routing failure destroys the run's output" outcome #35/#62 closed for `--return-dir`. Current tests pin the old behaviour, so the fix is ~4 lines plus test updates, not a redesign · Done when: a `secondary_dir` failure degrades like a best-effort mirror (or accumulates via `routing_failures`) and every canonical artifact is still emitted · refs `output.py` `_write_routed`, #35/#62 (the closed `--return-dir` half), ADR-43; escalated by Lane A1, deliberately out of its frozen contract
- [#76] [P2][S] **Verdict package can name a return copy that never landed** (Lane A1 escalation, 2026-07-19 integration; **Contract-Version 1.1 candidate alongside #34**): the package's own `artifacts[]` entry is built from `guaranteed_dirs` **before** its write (`output.py:790` precedes `:791`), so in accumulator mode the canonical package advertises a return-dir copy that the write then failed to produce — the manifest asserts a path that does not exist. Needs a **two-pass write** (serialize the manifest only after the writes it describes have landed), which is why it is a 1.1 candidate and not a 1.0 patch: it changes **when** the manifest is serialized on the delegation surface · Done when: `artifacts[]` names only destinations that actually received the file · refs `output.py:790-791`, #34 (the other 1.1 candidate — version them together), `scripts/verify_output_writes.py` L11 (already reports this as a standing **GAP**; that visibility is the interim mitigation, **not** a fix); escalated by Lane A1, deliberately out of its frozen contract
- [#78] [P2][S] **`target_paths` silently accepts destructive iterable shapes** (sol F3, `output.py:283`; PRE-EXISTING — `27a45d1:188` has the identical `for target_dir in target_paths or []:` with no validation): a `str` or `list[str]` becomes swallowed per-item errors, and a **generator is exhausted by the first writer**, so later deliverables silently mirror nowhere. Same class as the `OutputRoutingError` constructor footgun this session repaired — a wrong shape accepted silently instead of rejected loudly · Done when: `target_paths` is validated/materialized to `list[Path]` at the boundary and a wrong shape raises `TypeError` naming the type received · refs `output.py:283`, ADR-43, the `OutputRoutingError.__init__` guard (merge `74e8359`) as the established idiom
- [#79] [P3][S] **A failed metrics sidecar can still enter the verdict manifest** (sol F4, `orchestrator.py:224`; PRE-EXISTING and **already improved** — `27a45d1:208-210` built the path unconditionally with *no* check at all; A1 added an `.exists()` test): after `_save_metrics_json` reports failure, `metrics_path.exists()` is treated as proof of a successful artifact, and `output.py:899` repeats the same test — a stale or partial file from an earlier run satisfies it · Done when: manifest inclusion is driven by the write's own success signal, not by a post-hoc existence check · refs `orchestrator.py:224`, `output.py:899`, #63 (the sidecar-degradation path), `docs/audits/2026-07-19-codex-a1-failloud-adversarial.md`
- [#80] [P3][M] **Multi-line option payload is truncated to its first line** (terra pass 6 on the #77 arc, 2026-07-20; **PRE-EXISTING**, not introduced by #77 — main's `_top_level_bullets` drops indented lines by the same rule): `- Adopt PostgreSQL for\n  durable transactions` yields `Adopt PostgreSQL for`, losing the continuation. The indented-line rule is **deliberate** — it is what stops an ideas entry's `Who endorsed it` annotation being scooped as its own option (see `test_options_nested_sub_bullets_still_dropped`) — so this is a **design fork, not a bug fix**: a continuation line and a nested annotation are not distinguishable without a rule for which is which. Deliberately NOT fixed inside #77's frozen contract · Done when: **the CommonMark continuation rule is adopted** (ruled 2026-07-25, spike-proven) — a line continues the preceding option per CommonMark list-item continuation semantics (including lazy continuation) and does not continue it when it starts a new block, replacing the first-line truncation; the annotation-dropping test (`test_options_nested_sub_bullets_still_dropped`) stays green · refs `output.py` `_top_level_bullets`, #77, `docs/audits/2026-07-20-codex-opt-extractor-contract-pass6.md`
- [#81] [P3][S] **Fenced code blocks inside an options section fabricate options** (terra pass 6 on the #77 arc, 2026-07-20; **PRE-EXISTING**): a fenced diff whose lines start `- ` / `+ ` is read as list items, so `- delete legacy` becomes an authoritative `options_considered` entry although it is not a Markdown list item outside the fence. Same fabrication class as the F8 risks-as-options defect. Deliberately NOT fixed inside #77's frozen contract because the fix is **block-level** (the contract's surface is line-level) and it **inverts the failure mode**: if a model ever fences its options list, skipping fenced content turns fabrication into total option loss — needs a ruling on which failure is preferred · Done when: **the KEEP-SCANNER ruling is implemented** (ruled 2026-07-25; evidence at tag `spike/md-parser-evidence`, verified present) — the hand-rolled scanner is retained (no markdown-parser dependency) with a hand-ported fence-skip structure tracking fence open/close so lines inside a fence are never read as list items, and a fenced options list is shown not to be silently emptied · refs `output.py` `_top_level_bullets`, #77, F8 (the risks-as-options precedent), `docs/audits/2026-07-20-codex-opt-extractor-contract-pass6.md`
- [#69] [P2][S] **Frontmatter `models:` is dead in the default path, and the two entry points disagree** (#44 terra pass, 2026-07-19; VERIFIED against source): (a) interactive `--file` at `cli.py:719-721` gates the frontmatter `models` read on `not eff_full`, but `eff_full = (use_full_panel or not lite) or ...` at `:718` is **`True` whenever `--lite` is absent** (`--lite` is `default=False`), so a frontmatter `models:` is **silently discarded on every non-`--lite` run** despite `models` being a documented key in `settings.yaml` `council_frontmatter_keys` and despite the `:711` comment promising "CLI flag > frontmatter > config default"; (b) the **inbox path at `cli.py:607` guards on `not use_full_panel` instead** — a different condition that is `True` by default — so the **same brief file yields a different panel via `--file` than via `--inbox`** · Done when: the frontmatter `models` guard is correct and identical on both paths (a shared helper, per the inbox-parity anti-pattern in CLAUDE.md §10) · refs `cli.py:607`/`:718-721`, #22 (merge `3e64e81`), #64 (same `--file` surface — merge if fixed together); terra #44 review 2026-07-19 · **P2 CONSTRAINT (verified 2026-07-23, session-close archaeology):** the gating expression `(use_full_panel or not lite)` — now at `cli.py:689` (inbox `fm_full`) and `cli.py:812` (`--file` `eff_full`, with the `models` gate `not eff_full` at `:814`) — is the SAME expression that makes `full_panel` the effective 5-model default (ADR-02 Implementation section; panel-default archaeology, batch-report 2026-07-23 amendment). Any fix that changes its semantics to let frontmatter `models:` through can **silently flip the panel default 5→3**; the fix must decouple the models-gate from the panel-default resolution, and a panel-default regression test must pin the bare-invocation 5-model behaviour before the change lands
- [#64] [P3][S] `--file` frontmatter robustness: a malformed `--file` is not caught (`frontmatter.load` errors are unhandled; only `RoutingError` is), and an invalid frontmatter `mode:` falls through to auto-detection whose failure fallback is hardcoded `pick` rather than the configured `default: true` mode · Done when: malformed frontmatter fails cleanly and an invalid mode falls back to the configured default mode · refs `cli.py:695/728`, `mode_detector.py:50`, #22; verification (sol)
- [#100] [P3][M] **Verdict-package JSON Schema so a foreign repo can validate the machine-authoritative surface** (operator ruling 2026-07-25; **Contract-Version 1.1 cut, alongside #34/#76**): the verdict package is the Lane A deliverable, but `protocols/COUNCIL_INVOCATION_CONTRACT.md` describes its field set in prose only — a foreign caller has nothing to validate against, so a field rename or a type change is detectable only by a human re-reading the contract. Not built · Done when: a versioned JSON Schema for `council-verdict-*.json` ships in-repo, the CONTRACT names it as the machine-authoritative surface, and a test validates a really-emitted package against it with a seeded field/type violation demonstrably failing · refs `output.py` `save_verdict_package`, `protocols/COUNCIL_INVOCATION_CONTRACT.md`, ADR-13 (Contract-Version: a new machine surface is a MINOR bump at least), #34 + #76 (the other 1.1 candidates — version the three together, do not ship one alone)

### [S13] Stand up the commissioning window — council-side input stage + caller-facing docs
So that a delegating agent submits a raw question to the council-side input stage (`council boost`, ADR-11 amendment 2026-07-22, owner-C) and knows exactly where the outputs are and how to read them — retitled 2026-07-22 from "caller-side commissioning advisor" per the owner-C ruling (the caller submits, it does not author; RIDER 2 framing obsolete).
- [#88] [P3][S] Caller-facing `council boost` invocation note: how a foreign repo invokes the input stage — raw question in (inline or `--file`), brief file(s) out (`--out-dir`), exit 0/1/3 per ADR-08, caller then runs `council --file <brief>` itself — as a SECTION in the existing `protocols/COUNCIL_INVOCATION_CONTRACT.md`, NOT a new document · Done when: the contract carries a boost invocation section a foreign caller can follow without reading `boost.py` · refs ADR-11 amendment 2026-07-22 (owner-C), merge `3bf3ca9`; distinct from #38 (that is the output/read-back side) · successor of #36's one genuine remainder (struck 2026-07-22, operator ruling: superseded-by-boost)
- [#38] [P3][S] Outputs read-back guide (verdict→ADR): a documented caller path from the #26 verdict package to a recorded ADR (the F7 verdict→ADR §Q4 section-keyed template) — where the outputs are and how to read them · Done when: a caller can go from `council-verdict-*.json` to a drafted ADR using the template, with the F7 adoption-evidence gate noted · refs L-INT §3(Q4)/F7, DRAFT-INT-1 (#26 delivered the package); template artifact is hub-side, adoption gated on F7 evidence
- [#101] [P2][M] **Sharpening-annotation block in the emitted brief** (operator ruling 2026-07-25; bound to the P2 arc; **no ADR-11 reopen** — this is emit-only, the council never asks anything): `council boost` emits a conformant brief but says nothing about how the question could be made sharper, so the one cheap moment for narrowing passes silently. Add a delimited annotation block carrying three classes — (a) candidate narrowings, (b) missing-constraint questions, (c) criterion pushback — **questions and flags only: the block must never invent a fact, a constraint, an option, or an answer**, extending boost's verbatim-span discipline to the annotation surface · Done when: a boosted brief carries the block with those three classes, a test pins that no annotation asserts content absent from the raw question, and the block is inert to `inbox.parse_file` (parsed frontmatter and body contract unchanged) · refs `src/ai_council/boost.py`, ADR-11 amendment 2026-07-22 (owner-C), `docs/audits/2026-07-21-night-input-layer-audit-fable.md` §4 item 2 (the `assumed`-list-as-question-list result this implements), `docs/audits/2026-07-22-codex-council-boost-unit2.md`, #88 (the caller-facing note that must describe it), #113 (the interactive variant — deferred, different blocker, filed separately so this line can close on its own)
- [#117] [P2][S] **ADR-11 decision-1 interactivity: rule it either way** (F2 batch R7, ruled 2026-07-26 that the ruling must LAND, not that it must land a particular way): #113 (the boost interactive clarify-loop) is correctly blocked on this, and its done-when already names **both** exits — build Design B, or close as deliberately-not-doing with the ruling cited. What was missing is that **nothing owned the ruling itself**, so #113 could sit blocked forever on a precondition with no owner and no date. This line is that owner · The question: may a council-side stage ask the caller a question mid-run, or is Lane A fire-and-forget by contract? The structural fact either answer must survive: **Lane A has no ask-back channel** — the CLI blocks synchronously and speaks only exit codes and files — so "yes" costs either a two-phase file protocol or MCP elicitation, and "no" costs the clarify loop · Done when: the ruling is recorded in ADR-11 (dated in-body amendment marker, never an in-place rewrite — CLAUDE section 5 item 3) **and #113 has been driven to whichever of its two named exits follows** — this line does not close on the ruling alone, because a recorded ruling that leaves #113 open reproduces the exact zombie it exists to prevent · refs ADR-11 + its 2026-07-22 owner-C amendment, #113 (the blocked line), #101 (the emit-only sibling that deliberately needs no ruling), `docs/audits/2026-07-21-night-input-layer-audit-fable.md` "Design B" Conflicts note
- [#113] [P3][M] **Boost interactive clarify-loop — DEFERRED, blocked on the ADR-11 decision-1 interactivity ruling** (operator ruling 2026-07-25; filed as its own id rather than a rider on #101 precisely because its blocker and lifecycle differ — a clause inside #101 would either block that line's closure or vanish with it): boost currently emits and exits; the interactive variant detects underspecification, asks the caller ≤N (≈3) targeted questions, and folds the answers into the reformulation. The design exists — "Design B — bounded clarify-loop (the interviewer)" — and so does the structural obstacle it names: **Lane A has no ask-back channel at all** (the CLI blocks synchronously and speaks only exit codes and files), so this becomes either a two-phase file protocol (boost returns questions, caller answers, resubmits) or MCP elicitation. Do not build ahead of the ruling · Done when: the ADR-11 decision-1 ruling lands **AND either (a)** the bounded clarify-loop is built per Design B, **or (b)** the ruling forecloses interactivity and this task is closed as deliberately-not-doing, with the ruling cited — both exits are named so a ruling that goes the other way closes this rather than leaving it open forever · refs `docs/audits/2026-07-21-night-input-layer-audit-fable.md` "Design B" + its Conflicts note (ADR-11 if council-side and interactive; Lane B's fire-and-forget batch contract becomes a two-pass tray) + §5 (MCP elicitation as the missing channel), ADR-11 decision 1, #101 (the emit-only half this is the interactive variant of), CLAUDE.md §10 inbox-mirror blind spot (whichever mechanic ships must ship on both paths at once) · **FORCING FUNCTION FILED 2026-07-26 (F2 batch, R7):** the ADR-11 decision-1 interactivity ruling this waits on is now tracked as **#117** rather than sitting as an unowned precondition. Both exits here are already named, so #117 landing **either way resolves this line** — it cannot sit as a zombie once the ruling exists

---

## [E2] Synthesizer refresh
> As the tool owner, I want the default synthesizer chosen on real scoring data and the choice codified, so the verdict author is evidence-based and cost-aware.
> **Note:** this theme is the baseline gate for the Council-process theme's baseline-gated items.

_[S1] Decide the synthesizer on real data, then codify it — **delivered 2026-07-18, struck 2026-07-21** (#2 Branch A shipped `ca7e85c`, merged `6e83e41` — `config/settings.yaml:12` `synthesizer: "openai"`; #2's amendment half + #3's cost-optimization principle written `e3bdcc8`, merged `a854bd3` — the dated `Revised 2026-07-18 (Epic B — BACKLOG #2/#3)` marker in `ADR-01`, carrying **"Cost-optimization principle (#3): the default is chosen by balancing measured synthesis quality against per-run cost, not by assumption"** verbatim). Both done-whens re-verified against source before striking (`docs/audits/2026-07-20-night-backlog-audit-opus.md` kill-candidates table). The close had been split across two merges, each disclaiming the other's half — `6e83e41` said "ADR-01 amendment text deferred", `a854bd3` framed the amendment as a hygiene rider inside a filing pass — so neither struck the items. Done-tasks leave per ADR-65; git carries the record. Theme backbone retained._

### [S2] Settle the panelist/synthesizer overlap policy
So that overlap rules are explicit if the synthesizer ever joins the panel.
- [#4] [P3][S] Amend ADR-02 (overlap policy + stale stamp) — **re-scoped 2026-07-23** (condition fired 2026-07-21 when #2 Branch A shipped openai; escape hatch "closed as not-needed if Gemini retained" void; moratorium that held this re-scope was lifted mid-window, lift recorded in the grooming log this date): (a) codify the cost-reframed panelist/synthesizer overlap policy (openai is both a full-panel member and the synthesizer default — `exclude_synthesizer_from_panel`/`pick_synthesizer` fallback semantics deserve ADR text); (b) refresh ADR-02's `Deployment-Status` stamp, whose "No open remainder" claim predates this item; (c) **the panel-default question is explicitly DISSOLVED and out of scope** — the 2026-07-23 archaeology proved code, ADR-02's Implementation section, ARCHITECTURE, and the GUIDE mutually consistent (5-model effective default via `cli.py:689/812` `use_full_panel or not lite`; see the batch-report amendment) — the amendment must not reopen it · Done when: a dated ADR-02 amendment lands covering (a)+(b) via additive marker, README index synced (ADR-14) · refs ADR-02, ADR-01 (Revised 2026-07-18), `docs/audits/2026-07-22-pre-handoff-cleanup.md` AMENDMENT, #69's P2 constraint (same expression)

---

## [E3] Provider reliability & CLI engine
> As the tool owner, I want every wired provider to have a known-good, tested path and a route to CLI-subscription backends, so reliability is measured and cost is controllable.

### [S3] Close the untested and unreliable provider paths
So that no provider is wired-but-unverified or silently degrading the panel.
- [#5] [P2][M] Add an integration path for `openai_deep_research.py` (o3-deep-research, ~45 min, ~$10+/run — cannot run in CI) · Done when: a manual integration test path is documented + runnable · refs migrated from tasks/todo.md
- [#6] [P3][M] Evaluate whether DeepSeek should be replaced or demoted from the default full panel · Done when: a replace/keep/demote decision is recorded · refs reactive trigger: round-blocking failure rate >2% per JOURNAL data · **FORK RULED 2026-07-26 (F2 batch) — KEEP; convert this line to a WATCH rather than leaving it an open evaluation.** The done-when is reactive ("failure rate >2% per JOURNAL data") and **no trigger has fired**, so as a task it is a permanent zombie: nothing can close it and nothing should act on it. Its correct home is the DRAFT-GOV-2 `## Watches` block, which exists precisely for trigger-gated observations · **NO failure-rate measurement was taken for this ruling and none is claimed** — the ruling is about the item's SHAPE (watch, not task), not about DeepSeek's performance. Load-bearing context either way: CLAUDE section 5 item 10 forbids merging `deepseek.py` into `xai.py`, so DeepSeek is a first-class seat, not incidental · Done when: #50 lands the `## Watches` block and this becomes watch **W5** (trigger: round-blocking failure rate >2% across a JOURNAL-measurable window), at which point this task line leaves · **blocked on #50** — do not delete this line before the watch has somewhere to live
- [#20] [P3][S] OpenAI/Grok Responses-API type drift — real fix (stopgap applied): the unbounded `openai>=1.50` pin drifted to 2.14.0 (gpt-5.2 rollout), narrowing the `.create()` overloads + tool-param unions and typing annotation containers as `object` → 6 mypy errors in `research/providers/{openai_mini,openai_deep,grok}_research.py`. **Stopgap applied 2026-07-18** (`chore/openai-sdk-mypy-stopgap`): narrow per-line `# type: ignore[call-overload|attr-defined|misc,list-item]` so `check.ps1` mypy is green again; runtime unaffected. · Done when: the real fix lands — migrate the provider calls to the 2.x SDK typing (or set a deliberately bounded `openai` pin) and remove the stopgap ignores · refs `research/providers/*_research.py`, `pyproject.toml` `openai>=1.50` pin; stopgap surfaced during Stream-A #22 (2026-07-18) · **Scope widened 2026-07-19 — this is TYPE-STUB HYGIENE, not just the openai 2.x migration.** Second missing-stub gate failure of the same day: after the openai 2.x drift (stopgap `# type: ignore`s, above), `py -m mypy src/` failed again on `config/config_loader.py:8` — `Library stubs not installed for "yaml" [import-untyped]`, which the existing `ignore_missing_imports = true` does **not** cover (that flag handles `import-not-found`; PyYAML is installed and simply ships no `py.typed`). Fixed properly by declaring **`types-PyYAML>=6.0` in `[project.optional-dependencies] dev`** — no new ignore — restoring `check.ps1` to exit 0. Two stub-class gate failures in one day means the root cause is that **typing dependencies are undeclared until something breaks**; the real fix should audit every third-party import for a needed stub package and declare them in dev extras, not just unwind the openai ignores
- [#21] [P3][S] Fix or delete the stale integration test `tests/test_integration.py::test_full_debate_pipeline` (`ImportError: cannot import name '_build_all_providers' from 'ai_council.cli'` — the symbol no longer exists; the test is `@pytest.mark.integration`, deselected by the canonical unit gate `-m "not integration and not envcheck"`, so it never blocks `check.ps1`) · Done when: the test imports cleanly and passes, or is removed with rationale · refs pre-existing on main, surfaced 2026-07-11 during the #326 arc
- [#89] [P2][S] Degraded research reports are cached for 7 days (night-audit **P1-6**; re-verified live 2026-07-23): `research/runner.py` calls `cache_put` gated only on `no_cache` — a run where most providers failed (even one that exited 3 with the ADR-08 alarm) writes its degraded report into the 7-day cache, so the next same-query run silently serves the degraded result with no alarm · Done when: a degraded report is either not cached, or is cache-stamped degraded and re-triggers the ADR-08 alarm on a cache hit · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-6, `research/runner.py` (cache write after `summarize_report`), `research/cache.py`, ADR-08
- [#90] [P2][S] The research summarizer is the one network call with no timeout and no retry cap (night-audit **P1-10**; re-verified live 2026-07-23): `research/merger.py summarize_report` calls `client.chat.completions.create` bare — no `timeout=`, no retry bound — so a hung summarizer endpoint stalls the whole research run indefinitely after every provider already finished · Done when: the summarizer call carries an explicit timeout (config-sourced) and a bounded retry, failing soft to the unsummarized merged report · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-10, `research/merger.py:168-180`, `config/settings.yaml` research block
- [#91] [P2][S] Research timeout status is detected by substring-sniffing the error message (night-audit **P1-11**; re-verified live 2026-07-23): `research/display.py` classifies `statuses[name] = "timeout" if "Timed out" in str(exc) else "error"` — a case or wording change in any provider's message silently reclassifies timeouts as generic errors; the audit found the sniffed substring is not what several providers emit · Done when: timeout classification rides a typed signal (exception class or an explicit field on `ResearchProviderError`), not message text · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-11, `research/display.py:117-127`, `research/provider.py`

### [S4] Add a CLI-subscription provider backend
So that subscription CLIs can serve debate turns and API spend is reserved for CLI-less models.
- [#27] [P3][M] CLI-4 parity run → default-flip decision: n=12 stratified paired debates (CLI vs API), sealed-key blind, rubric-scored, non-inferiority 1/12 with zero margin on items 2 and 4; ratify DRAFT-CLI-3 or retire per its kill condition · Done when: the parity report exists and the flip decision (ratify/retire) is recorded, **AND — at unseal — the `2026-07-18-cli4-parity` row is retired from the `docs/audits/README.md` "Live corpora" table and the corpus is moved to `docs/audits/archive/`, leaving the parity report as its essence markdown at the audits root.** *Obligation NARROWED 2026-07-19 once #68 was proven armed:* the **register-while-live** half is **struck** — #68 (`scripts/validate_docs_registry.py`) now blocks any unregistered live corpus in `docs/audits/`, so the table can no longer rot in that direction. The **retire-the-stale-row-at-unseal** half above is **RETAINED and is NOT mechanised** — #68 catches a corpus with no row, but *not* a row with no corpus, and this is deliberate: a stale-row check needs existence/disk semantics, and the co-registered `2026-07-17-epi1-archaeology/` is gitignored, hence absent in every worktree and present only in the primary checkout, so such an arm would false-block every worktree commit (finding F-B, `docs/audits/2026-07-19-guards-violation-proof.md`). **Do not strike this clause believing #68 covers it.** Revisit only if a **third** live corpus is ever registered, and then in a place with full working-tree access (`check.ps1` / `verify_*`), never in a pre-commit guard. The unseal must also drop the then-dead `.gitignore:65` `SEALED-KEY.json` rule — **but that edit MUST NOT land in the same commit as the corpus move.** Rewriting an ignore rule in the same change as a relocation is the exact condition that produced the 2026-07-18 near-leak (a sealed key left untracked-but-unignored). Sequence: **move first → verify the key is still ignored → drop the ignore line only once the key is out of the tree.** This supersedes the earlier "per the epi1 §6 procedure any corpus move rewrites its ignore rules in the same change" instruction, which pointed the wrong way · refs L-CLI §3(Q2)/§4(DRAFT-CLI-3), ADR-12 §5, `docs/audits/README.md` (directory invariant + registry), #68 · prerequisite #16 (CLI seats) delivered 2026-07-17 · **de-risk (2026-07-18):** zero-fallback CLI-seat E2E already witnessed (night batch: claude+codex participate end-to-end at $0; ARC-4 witness run reconfirmed) — the CLI lane is proven ahead of the parity run
- [#43] [P3][S] First-class `codex` seat name in the provider registry: `PROVIDER_CLASSES` (`cli.py`) has no `codex` name, so a codex CLI seat must ride a non-openai model name (the night batch hosted it on `deepseek` because `openai` was the synthesizer) — a label/identity mismatch in `seats[]` · Done when: a codex CLI seat can be configured under its own truthful seat name without riding another provider's identity · refs `docs/audits/2026-07-17-night-batch-empirical-e2e-audit.md` §1.2, `cli.py` `PROVIDER_CLASSES`, `seat_router.py` `CLI_PROVIDER_CLASSES`; ADR-12 · **design fork (recon 2026-07-18):** codex has no API provider twin, so a first-class `codex` seat name breaks ADR-12's same-seat API-fallback assumption (every seat name maps to an API class) — the fix must decide the no-API-twin fallback semantics (no-fallback sentinel vs. mapping), not just add a registry entry; sized **M** · **FORK RULED 2026-07-26 (F2 batch) — NO-FALLBACK SENTINEL, not a mapping.** Mapping `codex` onto another provider's API class at the fallback layer would re-create, one level down, exactly the identity-riding defect this ticket exists to remove: the seat answers as itself until it degrades, then silently answers as another vendor. Under ADR-03 blind voting that is worse than a hard failure — the panel cannot see the substitution, so a vote gets counted against a provider that never spoke. A seat therefore declares **no API twin**, and its failure is a seat-level degradation (drop the seat, surface it in `seats[]` / `fallback_events`), never a silent identity swap · **Consequence, stated so it is not discovered mid-build:** ADR-12's same-seat API-fallback assumption is the thing that is wrong, so this needs an **ADR-12 amendment**, not just a registry entry — and ADR-12 section 5 is already gated on #27, so sequence the amendment with that unseal rather than opening ADR-12 twice

### [S11] Stand up the liveness surface (doctor)
So that seat/provider health is observable without spend and key hazards cannot silently gate runs.
- [#32] [P3][S] doctor-v2 CLI-fleet auth-lane check — **#16 waived-gate residual** (architect ruling 2026-07-17; terra review finding VERBATIM): *"CLI authentication mode is not verified — Scrubbing environment keys does not guarantee subscription authentication because both CLIs can use API-key credentials stored under the allowlisted home/config directories. A normally configured Codex or Claude CLI previously authenticated with an API key can incur API charges while `metrics.py` records every CLI call as `$0`. Fix direction: Admit the CLI lane only after verifying subscription/OAuth authentication; otherwise fall back to API accounting or reject the seat."* · Done when: the doctor (or seat-build) verifies each CLI seat's auth lane (subscription vs API-key) and the `$0` CLI-cost recording is gated on witnessed subscription auth (else API accounting) · refs the documented assumption at `src/ai_council/metrics.py` `build_call_metrics` (which names this item back); ADR-12 §Decision 3 CLI-fleet; doctor-v2 · **2026-07-18 (hit live, #27 Phase 0):** `council doctor` witnesses API seats only — it never probes the CLI subscription lane, so CLI-seat `$0` admission cannot be doctor-verified today; #27's rider (a) was amended to witness via a live CLI debate's `seats[]` sidecar instead. This item is the fix that would let the doctor do it.
- [#72] [P3][S] Health-record prune deletes by a broader glob than its contract (#44 terra pass, 2026-07-19; VERIFIED, severity downgraded from terra's High): `doctor.py:275-277` selects `health_dir.glob("doctor-*.json")` excluding only `doctor-latest.json`, then unlinks all but the newest `keep`. The docstring scopes retention to timestamped `doctor-<ts>.json` records, but the glob sweeps **any** `doctor-*.json` — a non-record file dropped in `health/` (e.g. `doctor-2020-operator-note.json`) sorts lexically oldest and is deleted first. **Downgraded to Low/Medium:** `health/` is a tool-owned directory so a foreign file is unlikely; filed because an over-broad `unlink` is unrecoverable · Done when: the prune matches only the timestamped record shape it claims (anchored regex, not a wildcard glob) · refs `doctor.py:275-277`, #39 (merge `e140c5d`), #65 (same doctor output surface — merge if fixed together); terra #44 review 2026-07-19

### [S5] Cut multi-round input-token cost
So that repeated brief/persona blocks don't re-bill on every provider call and debate round.
- [#85] [P3][S] ai-council prompt caching — apply `cache_control` to the repeated brief/persona blocks reused across providers and debate rounds, cutting input-token cost on multi-round debates · Done when: a council run reuses cached brief/persona blocks and the input-token saving is recorded · refs Anthropic API prompt-caching docs, hub #96 (CLOSED upstream 2026-07-20; **HUB** id space, not a local id — kept qualified rather than dropped so the provenance survives), #84 (this repo — renumbered from #110 in the same pass) · re-filed from the hub backlog 2026-07-08 (ADR-41 move, hub commit ea6217a) · **RENUMBERED #128 → #85 on 2026-07-26** into the local id space, discharging the reservation held since 2026-07-22; the hub id is retained here as provenance only

### [S16] Clear the small deferred nits
So that the low-severity remainders surfaced across the audit set are tracked rather than lost (grouped for grooming convenience; each is independently small).
- [#52] [P3][S] DRAFT-DOC-2 pin-aging lifecycle: the doctor surfaces superseded-model pins but does not age them — persist `advisories[].first_seen` (aging) and `held(<date>,<reason>)` markers between runs so a YELLOW advisory accrues age instead of resetting each run · Done when: the doctor persists first-seen/held state and an aging advisory shows elapsed time · refs `docs/intake/2026-07-06-lane-doc-functional-design.md` DRAFT-DOC-2 §4, `src/ai_council/doctor.py`; the surface→age→decide half of DOC-2 (surface half shipped #25)
- [#53] [P3][S] Question-misclassification guidance (framing-bias audit R4): the corpus held a directive submitted as `research` mode that failed all six framing checks; add auto-mode-detection or guidance on what is *not* a research question so a directive is not run as research · Done when: a directive-shaped `research` brief is flagged or re-routed, or the guidance is documented in the GUIDE · refs `docs/audits/2026-05-17-question-framing-bias-audit.md` R4, `protocols/COUNCIL_QUESTION_GUIDE.md`
- [#54] [P3][S] Research `"incomplete"`-as-terminal nuance: `_TERMINAL_STATUSES` treats an autonomous-agent run ending `"incomplete"` as a hard `ResearchProviderError`, so a legitimate partial counts as a full failure · Done when: an `"incomplete"` terminal is distinguished from a hard failure (partial-result path or explicit re-classify) · refs `docs/audits/2026-05-18-research-provider-health-check.md`, `_TERMINAL_STATUSES` in the research provider layer
- [#59] [P3][S] Research slug bare-`"research"` doubling: a query that is (or normalizes to) bare `"research"` — e.g. `"research"`, `"research!"` — does not match `_LEADING_RESEARCH_RE` (which requires a separator + content), so the mode token still doubles into `...-research-research.md`; #42 fixed only the separator+content case · Done when: a bare-`"research"` query yields a single `research-` token · refs `research/output.py` `_LEADING_RESEARCH_RE`/`_slug`, #42; `docs/audits/2026-07-19-night-consolidation-verification.md` (sol)
- [#61] [P3][S] Partial CLI usage booked as zero tokens: a successful claude CLI response with partial `usage` (e.g. `input_tokens: null` and no output count) is converted to a definitive `token_count=0`, which `metrics.py` books as zero rather than preserving unknown-usage — #41 records real counts only when usage is complete · Done when: partial/unknown CLI usage is distinguished from a genuine `0` · refs `cli_base.py:283`, `metrics.py:28`, #41; verification (sol + terra T2)
- [#82] [P3][M] Research-lane retrieval cost is invisible to `DebateMetrics`, so `print_cost_summary` understates true spend: `ResearchResult.cost_usd` (`research/models.py:21`) / `MergedResearchReport.total_cost_usd` are a **separate ledger** from `ProviderCallMetrics`/`DebateMetrics` (`models.py:86-108`), which only aggregates `AIProvider` calls. The #18 crux-check makes this load-bearing — it fires a headless retrieval on **every** debate, so a `pick` run now spends real money on research providers that the cost summary never shows. Registering them means synthesizing `ProviderCallMetrics` from `ResearchResult` across a different provider namespace (`research.providers` vs top-level `models:`) with no input/output token split on several providers · Done when: a debate that ran a crux-check reports total spend including its retrieval calls, or the two ledgers are explicitly reconciled in the cost summary · refs `metrics.py:54-84` (`build_debate_metrics`), `output.py:108` (`print_cost_summary`), `research/models.py:15-40`, #18 (accepted Phase-A hole E2), sibling of #61 (same metrics-fidelity class)
- [#70] [P3][S] Research report filename can silently overwrite a sibling (#44 terra pass, 2026-07-19; mechanism VERIFIED, severity downgraded from terra's High): `research/output.py:50-55` builds `council-out-{ts}-research-{slug}.md` from a **second-resolution** `ts` and a slug **truncated to 50 chars**, and the write is unconditional — two research runs starting in the same second whose queries share a 50-char normalized prefix produce the same path and the later silently replaces the earlier. **Downgraded to Low:** a research run spans minutes across providers, so same-second starts are remote; filed because the loss is silent and unrecoverable, not because it is likely · Done when: the filename is collision-proof (sub-second precision, a short query hash, or refuse-to-overwrite) · refs `research/output.py:50-55`, #42 (merge `5b139ad`), #59 (same slug surface — merge if fixed together); terra #44 review 2026-07-19
- [#66] [P3][S] Live witness of `--no-persist` / `AICOUNCIL_OUTPUT_DIR` on a `run`: the #39 output guard is proven for retention + resolution-code offline, but the run-path scratch/env redirect is only reachable through a paid debate and was not exercised · Done when: a real `run` with `--no-persist` (and one with `AICOUNCIL_OUTPUT_DIR`) is witnessed writing outside canonical `./output/` · refs `cli.py:517-529`, #39; verification GAP (L4c) · **2026-07-19 (Lane A2): a discharge leg EXISTS but is un-runnable at $0 — `scripts/verify_cli_output_contract.py` L11 is the attributed leg, gated on `AICOUNCIL_LIVE_WITNESS=1` and reporting GAP by default.** The lane's working assumption that the witness could run free on CLI seats was **falsified against the committed config**: `config/settings.yaml` declares **no `backend: cli` seat**, `seat_router.py:134` defaults `requested_backend="api"`, and there is **no `codex` seat**, so any live run bills the API. L11 therefore prices the run from live config and refuses to spend unless `AICOUNCIL_LIVE_WITNESS_BILLED=1` also confirms the cost is accepted · **unblocks via #27** (the ADR-12 §5 `backend=cli` flip) **or** an explicitly authorized billed run
- [#86] [P3][S] GUIDE frontmatter-table synthesizer staleness: `protocols/COUNCIL_QUESTION_GUIDE.md` (frontmatter table + the preceding template note) still names `gemini` as the synthesizer default; the live default is `openai` since 2026-07-18 (ADR-01 in-body amendment) · Done when: the GUIDE's synthesizer references match the live default or defer to config, with no hardcoded stale default · refs ADR-01 (Revised 2026-07-18), `docs/audits/2026-07-17-synthesizer-ruling-gemini-to-openai.md`; surfaced by Unit 2 (boost deliberately emits no synthesizer key as the guard) · renumbered from #84 2026-07-22 (id-reservation collision)
- [#87] [P3][S] GUIDE `rounds:`-in-template vs `ideas` mode-default divergence: the canonical decision template pins `rounds: 2` while frontmatter rounds outranks the mode default (`cli.py:882-890` precedence), so any template-conformant brief auto-detected as `ideas` runs 2 rounds instead of its configured `max_rounds: 1` — boost inherits this by emitting `rounds:` on decision briefs (terra 2026-07-22 HIGH-2, declined at boost level, deferred here) · Done when: a template-level ruling is recorded (drop `rounds:` from the template, or accept the pin) and template + boost aligned with it · refs `protocols/COUNCIL_QUESTION_GUIDE.md` §The Format, `docs/audits/2026-07-22-codex-council-boost-unit2.md` HIGH-2, `src/ai_council/boost.py` in-code note · renumbered from #85 2026-07-22 (id-reservation collision) · **RULED 2026-07-26 (F2 batch, R9) — DROP `rounds:` from the canonical template; the mode defaults stay authoritative.** A template pinning `rounds: 2` silently overrides every mode's configured `max_rounds`, so a template-conformant brief auto-detected as `ideas` runs 2 rounds against its configured 1. That is config living in two places with the DOC winning — CLAUDE section 5 item 8 inverted (`settings.yaml` owns the numbers). Dropping the pin lets the mode default apply and **keeps `rounds:` available as a deliberate per-brief override** for a caller who genuinely wants a different count: the precedence chain is unchanged, the template merely stops exercising it by accident · **Executor ruling under the F2 batch mandate**, reversible while it is still a BACKLOG record · Done when: `protocols/COUNCIL_QUESTION_GUIDE.md` section The Format no longer pins `rounds:`, `boost.py` stops emitting it on decision briefs, and a test pins that a template-shaped `ideas` brief runs its configured `max_rounds`

---

## [E4] Model currency
> As the tool owner, I want to know when the configured panel models fall behind the latest releases, so the Council never silently debates on stale models.

### [S6] Detect stale model configuration
So that a superseded model in settings.yaml is surfaced, not silently used.
- [#17] [P3][M] Online model-version check: verify the `config/settings.yaml` model strings are the latest available per provider + a documented update process · Done when: a check reports any configured model that is no longer the latest + the refresh process is written down · refs `config/settings.yaml` is the single source of model strings
- [#57] [P3][S] Bump the durable claude API pin `claude-opus-4-7` → `claude-opus-4-8` in `config/settings.yaml` (newest-models policy — opus-4-8 is the current Opus, 4-7 is stale) · Done when: the claude seat's `model` string reads the current Opus and a run confirms it resolves · refs `config/settings.yaml` claude seat; surfaced 2026-07-18 during #27 pin selection — a **separate arc** from the parity trial's trial-scoped pins (operator ruling)

---

## [E5] Naming & quality automation
> As the tool owner, I want the ADR-34 naming convention enforced mechanically and its edge cases resolved, so violations are caught by CI, not reviewer luck.

### [S7] Enforce hyphen-only naming and resolve its timestamp edge case
So that new files cannot drift from ADR-34 and the ISO-timestamp ambiguity is settled.
- [#7] [P2][M] Add a CI check (pre-commit hook or ruff plugin) rejecting new `docs/`/`src/` files with UPPERCASE or underscores in the slug · Done when: a non-conforming new filename is blocked · refs strażnik finding I5 (SYNTHESIS-QUALITY-RUBRIC.md violation)
- [#8] [P3][S] Decide whether ADR-34 applies to ISO timestamps inside filenames (the `council-out-YYYYMMDD_HHMMSS` underscore) — fix the emitter to hyphens, or amend ADR-34 with an ISO-timestamp exemption · Done when: the methodology decision is recorded + applied · refs ADR-34 · surfaced by #7 once built · adjacent to #14 (same emitter) · **RULED 2026-07-26 (F2 batch, R9) — amend ADR-34 with an ISO-timestamp exemption; do NOT change the emitter.** The underscore in `council-out-YYYYMMDD_HHMMSS` is doing semantic work: it separates date from time INSIDE one token, while hyphens separate tokens. Hyphenating it (`council-out-20260607-124757-pick-<slug>`) makes the boundary ambiguous — neither a reader nor a parser can tell where the timestamp ends and the slug begins, which is a real loss bought for pure-hyphen purity. Changing the emitter would also orphan every existing `output/council-out-*` filename. ADR-34 governs the separator-BETWEEN-tokens axis; a compact ISO timestamp is one token · **Executor ruling under the F2 batch mandate**, reversible in one line while it is still a BACKLOG record — say so now if it should go the other way · Done when: ADR-34 carries the ISO-timestamp exemption as a dated in-body **amendment marker**, never an in-place rewrite (CLAUDE section 5 item 3)
- [#109] [P3][S] **`verify_*` vs `validate_*` is an undocumented convention** (#97 Unit 1 follow-up): `scripts/` holds five `validate_*.py` (audit_casing, backlog, claims, docs_registry, sealed_keys — all commit-gates or gate candidates) and six `verify_*.py` (cli_output_contract, night_consolidation, openai_deep, openai_mini, output_contract_e2e, output_writes — all re-runnable discharge/witness legs), plus three that follow neither (`canonical_freshness_gate.py`, `normalize_headers.py`, `session_end_backpressure.py`). The split is **real and consistent but lives only in the filenames** — no document states it, so the next script picks its prefix by coin-flip and the distinction rots. JOURNAL:1191 names a different axis (worktree provisioner stubs) and is not the record · Done when: the convention is either written down (ARCHITECTURE §Validators, with the three exceptions named) or **deliberately dropped** with the record saying so — not left emergent · refs `scripts/`, `ARCHITECTURE.md:418`, ADR-34 (filename conventions — this is the prefix axis, not the casing axis), #73 (the adjacent undecided runner convention — same class, decide them together if convenient) · **FORK RULED 2026-07-26 (F2 batch) — WRITE IT DOWN; do not drop it.** The split is already real, consistent and load-bearing (5 `validate_*` = commit gates or gate candidates, 6 `verify_*` = re-runnable discharge/witness legs), so dropping it discards a working convention and guarantees the next script picks its prefix by coin-flip — the exact rot this ticket names. Writing it down costs one paragraph · Done when: `ARCHITECTURE.md` section Validators states the `validate_*` = gate / `verify_*` = witness axis and **names the three current exceptions** (`canonical_freshness_gate.py`, `normalize_headers.py`, `session_end_backpressure.py`) as exceptions, so they stop reading as violations

_[S14] Clear the code-quality audit residue — **delivered 2026-07-18** (#45 re-export shim `f7f8227`, #46 utcnow→`iso_now()` `da7825c`, #47 dead `_target_projects` `6d55070`, #48 RunPolicy-from-`settings.yaml` `6c73686`; merged `02b6dd0`; `check.ps1` green 543. terra WAIVED to 2026-07-23 — carried by #44). Done-tasks leave per ADR-65; git carries the record. Theme backbone retained._

### [S17] Absorb the night-audit structural and test-trust findings
So that the audit-flagged oversized modules are refactored toward their stated invariants and the tests that assert nothing stop counting as coverage (successor class to the delivered [S14]; source `docs/audits/2026-07-20-night-code-audit-opus.md`, absorbed 2026-07-23 after first-hand re-verification of each finding).
- [#92] [P2][L] `cli.run()` is a 196-statement, 56-branch, complexity-50 function (night-audit **P1-12**; re-verified 2026-07-23 — `cli.py` is 1081 lines and `run()` still does provider loading, health checks, mode resolution, inbox processing, research dispatch, panel selection, runner construction) — **filed with the invariant attached:** ARCHITECTURE invariant 2 ("`cli.py` performs no business logic — it translates CLI arguments into a `RunRequest` and immediately delegates to `CouncilRunner`") is currently FALSE as written; per operator ruling the invariant stays as the **target** and this ticket is the refactor toward it — do not quietly reword the invariant to match the code · Done when: `run()` is decomposed so `cli.py` translates arguments and delegates (orchestration moved behind `CouncilRunner`/helpers), and ARCHITECTURE invariant 2 reads true against source, **AND the ARCHITECTURE allowed-edge set + current-state note are re-derived from source in the same arc** — the FLOOR1 R1 trigger (merge `b1f3319`) recorded the obligation as doc prose at `ARCHITECTURE.md:180-181` ("Re-derive this note after #92 lands") and no task carried it until 2026-07-25; the 10 unaccounted `cli` edges must either disappear with the decomposition or become explicitly ruled additions, and the set is **not** widened to match whatever remains · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-12, `cli.py:552`, ARCHITECTURE.md §Invariants item 2; sibling of #69 (same entry-point surface — coordinate, don't collide)
- [#118] [P3][S] **ARCHITECTURE writes paths base-relative without declaring the base — 16 occurrences** (surfaced by the 2026-07-26 rule-2 guard measurement; **file-only, deliberately not built**): `ARCHITECTURE.md` names `research/merger.py`, `providers/`, `decisions/`, `audits/`, `handoffs/`, `transcripts/` and similar, meaning them relative to `src/ai_council/`, `docs/` or `docs/decisions/` — but nothing in the document says so. A reader (or a checker) resolving them against the repo root finds nothing. The old rule-2 guard hid this by skipping every such token; #116 replaced it with a resolution model that carries the base list **in the checker**, which makes the tokens resolve but leaves the CONVENTION undeclared in the document that uses it — the base list is now the only place the convention exists, which is the wrong home for it · Done when: either ARCHITECTURE declares the base-relative convention explicitly (naming which sections are relative to which base), or the paths are written in full — **and `_R2_BASES` in `scripts/validate_claims.py` is reconciled to whichever wins**, so the checker follows the document rather than substituting for it · **Do NOT resolve this by adding bases** — each new base widens what silently resolves, and the fourth (`docs/decisions/`) was already added under acceptance pressure rather than from a declared convention · refs `ARCHITECTURE.md` (the 16 occurrences are listed in the 2026-07-26 drop-set measurement, JOURNAL), `scripts/validate_claims.py` `_R2_BASES`, #116, #109 (adjacent undeclared-convention class — decide them together if convenient) · **COLLISION COST, recorded 2026-07-26 (terra pre-merge review, HIGH-2):** because a token carries no source-base context, it is accepted if it resolves under **any** declared base — so a genuinely-missing root-relative `research/merger.py` passes on the strength of `src/ai_council/research/merger.py`. That is not a defect introduced by #116, it is the inherent price of the ruled base model, and it is **why this ticket exists**: the fix is to declare the convention (which sections are relative to which base) so bases can be bound to context, not to keep widening a context-free list. Deliberately NOT patched inside #116 · Done when (addendum): a collision test exists — a doc citing a path that resolves ONLY under an unintended base is a finding
- [#125] [P2][S] **Rule 4 reads ONE roster surface while its spec claims set-equality across FOUR — the LESSONS class, inside the checker itself** (found 2026-07-26 by a stale roster that produced no finding at FINDINGS 0): #97 specifies rule 4 as "ADR files on disk == CLAUDE §11 == ARCHITECTURE Governing-ADRs == `docs/decisions/README.md` index". The implementation's `_adr_roster_docs` returns **`("CLAUDE.md",)` alone** (`scripts/validate_claims.py:577-578`), so it compares the ADR files in HEAD's `docs/decisions/` against **CLAUDE.md only**, in one direction (disk → roster). `ARCHITECTURE.md` and `docs/decisions/README.md` are **never read by rule 4** · **Witnessed, not theorised:** ADR-15 landed 2026-07-26 in CLAUDE §11 and the README index but NOT in ARCHITECTURE's Governing-ADRs roster or its `ADR-01…14` header span, and the checker reported `pass 4 | FINDINGS 0` throughout — a genuinely stale canonical surface that the rule claiming to guard it could not see. Rule 3 already reads two docs (`("CLAUDE.md", "ARCHITECTURE.md")`), so the narrowness is rule 4's alone, not a harness limit · **This is LESSONS 2026-07-26 turned on the checker:** "rule 4 passes" is a value published without its predicate — the predicate is "CLAUDE.md agrees", not "the four surfaces agree", and nothing in the report says so · Done when: `_adr_roster_docs` covers every surface the spec names (at minimum + `ARCHITECTURE.md`; the README index is a TABLE, so it needs a row-parse rather than a whole-doc mention test — decide whether that is rule 4 or a sibling leg), a seeded stale roster in EACH covered surface is demonstrably caught, and #97's rule-4 text and the report agree on how many surfaces are actually compared · **Do not silently narrow the SPEC to match the code** — the four-surface claim is the requirement; the code is the thing that is wrong · refs `scripts/validate_claims.py:577-578` (`_adr_roster_docs`) + `rule_4`, `_hook_roster_docs:489-490` (the two-doc precedent), #97 rule 4, #51 (ADR-14 header↔index sync — the README half this would mechanise), LESSONS 2026-07-26
- [#93] [P2][L] `output.py` is four modules wearing one name (night-audit **P1-13**; re-verified 2026-07-23 — 1410 lines owning routed writes, verdict-package schema, markdown parsing/extraction (`_top_level_bullets`), and Rich presentation) · Done when: the write/routing, verdict-schema, extraction, and presentation concerns live in separately-testable units (module split or explicit internal seams), with the #77/#80/#81 extractor forks landing in the extraction unit · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-13, `output.py` (1410 lines), #77/#80/#81 (extractor arc — same surface, coordinate)
- [#119] [P2][S] **Crux-check refusal detection rejects valid claims — `"AI cannot"` matches the marker `"i cannot"`** (terra pass 3, 2026-07-26; **reproduced first-hand before filing**): `crux_check.py:137` does bare substring matching over `_REFUSAL_MARKERS`, so the claim *"Whether an AI cannot determine tumour grade from images is the crux"* matches **two** markers — `i cannot` (inside "AI cannot") and `cannot determine` — and is classified `MALFORMED`. A legitimate crux is silently discarded and reported as retrieval-unavailable, so the failure is **invisible at the call site**: it looks like the provider failed, not like the parser rejected a good answer. Introduced by the pass-2 repair that routed `_REFUSAL_MARKERS` to MALFORMED — the repair was right in intent and too broad in mechanism · Done when: refusal language is matched with **word boundaries** (or at response-level prefix position) rather than as arbitrary substrings, and a test pins the "AI cannot" claim as a VALID crux · refs `src/ai_council/crux_check.py:67-80` (`_REFUSAL_MARKERS`) + `:137`, `docs/audits/2026-07-26-codex-crux-check-pass3.md`, #18 (the arc), #83 (discharged by filing this)
- [#120] [P2][S] **`CruxService.check()` documents "Never raises" and catches only `ProviderError`** (terra pass 3, 2026-07-26; **confirmed by reading source before filing**): the docstring at `crux_check.py:203` states the method never raises, but `:212-218` guards the extractor call with `except ProviderError` alone. A timeout, or any provider-implementation bug that raises something else, escapes `check()` and breaks the contract its own callers rely on — the same fail-loud-vs-degrade class as the #97 checker's H1, but inverted: here the claim is **never raises** and the code can · Done when: expected extractor-call failures degrade to `RETRIEVAL_UNAVAILABLE` while **cancellation semantics are preserved** (do not swallow `CancelledError` — a blanket `except Exception` would trade a contract violation for a hung task), and a test pins a non-`ProviderError` extractor failure returning an artifact rather than raising · refs `src/ai_council/crux_check.py:203`/`:212-218`, `docs/audits/2026-07-26-codex-crux-check-pass3.md`, #18, #83 (discharged by filing this)
- [#94] [P2][S] `test_skips_failed_results` is a literal tautology (night-audit **P1-14**; re-verified 2026-07-23): its final assertion `"fail" not in report.merged_report.upper() or "fail" in report.merged_report.lower()` is always true (the first clause can only be false if the report contains uppercase-insensitive "fail", in which case the second clause holds), so the test cannot fail on the behaviour it names · Done when: the test asserts the failed provider's content is absent and the error is surfaced where the contract says, and can demonstrably fail when the skip logic is broken · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-14, `tests/test_research.py:146-155`
- [#95] [P2][S] The ADR-03 blind-voting test asserts nothing about anonymization (night-audit **P1-15**; re-verified 2026-07-23): the capturing wrapper installed on `p.generate` is immediately overwritten by the subsequent side-effect re-set loop (`tests/test_debate.py:185-218`), so `captured_prompts` never captures and the test passes without inspecting a single round-2 prompt — the blind-voting contract (CLAUDE §10, `_anonymize_responses`) has no effective test · Done when: the round-2 prompts are actually captured and asserted anonymized (no provider names, shuffled labels), and breaking `_anonymize_responses` fails the test · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-15, `tests/test_debate.py:185-218`, ADR-03
- [#124] [P2][M] **REPRODUCIBILITY: the declared dependency ranges do not reproduce a green gate — a clean-room install fails `mypy` today** (surfaced 2026-07-26 by building the first per-worktree venv): a fresh `pip install -e ".[dev]"` from `pyproject.toml` resolves **mypy 2.3.0** and **google-genai 2.14.0**, and `mypy src/` then reports **3 errors in 2 files** — `inbox.py:137` (`resolve` arg-type) and `gemini_research.py:88`/`:96` (the genai 2.x `AsyncInteractions` / `Interaction | AsyncStream` union changed shape). The primary's gate is green only because the interpreter it happens to use holds **mypy 1.19.1** and **google-genai 1.73.1** — versions nothing in the repo pins · **DISTINCT FROM #123, and the two must not be collapsed:** #123 is *which interpreter runs the gate* (bare `pytest`/`mypy`/`python` resolve to the system install rather than the repo venv) — solvable by repointing callers, and it would leave THIS defect completely intact. **This ticket is that the declared ranges do not reproduce a green gate at all**, so #123's fix makes the failure *visible* rather than fixing it: repoint `check.ps1` at a venv built from these floors and the gate goes RED on the first run. Sequence accordingly — this is a **prerequisite** for #123, not a sibling · **Same class as LESSONS 2026-07-26** (a value published without the predicate that produces it): "the gate is green" is published with its real predicate — *on this machine, with these accidentally-held versions* — left unstated, so the signal cannot be reproduced by anyone else and cannot be trusted by us · Done when: the toolchain and provider deps carry ranges that actually type-check (pin/cap `mypy` and `google-genai`, or fix `src/` against the newer APIs — a decision, not a default), and a **clean-room install proves it**: a fresh venv from a bare clone runs `check.ps1` green with no manual pinning · **Do not "fix" this by pinning to whatever the system happens to hold** — that would freeze the accident into the declaration and lose the newer-API question rather than answer it · refs `pyproject.toml` `[dev]` extras (`mypy>=1.10`) + the google-genai dependency, `src/ai_council/inbox.py:137`, `src/ai_council/research/providers/gemini_research.py:88`/`:96`, **#123 — THIS TICKET BLOCKS IT (operator ruling 2026-07-26)**, not merely "relates to": #123 repoints `check.ps1` and the hooks at `.venv`, and a `.venv` built from the floors this ticket indicts is a RED gate, so #123 executed first breaks the green gate on the day it lands and mis-attributes the breakage to itself. Land #124 first and #123 becomes a safe repoint; land #123 first and it becomes an incident · refs #20 (the earlier type-stub hygiene item, same family)
- [#98] [P2][M] The two orchestration functions are mocked out of existence in every test that names them (night-audit **P1-16**; re-verified 2026-07-23): orchestrator tests patch `run_debate`, `synthesize`, AND the writers (`tests/test_runner.py:218-235`), so the sequencing code between them — exactly where P1-5 and P1-9 lived — is exercised by no test · Done when: at least one integration-shaped unit test runs `CouncilRunner.run` with real orchestration and only the provider/network boundary faked, covering the synthesis-failure and write-failure sequencing paths · refs `docs/audits/2026-07-20-night-code-audit-opus.md` P1-16, `tests/test_runner.py:218-235`, the P1-2/P1-9 in-code repair comments (`debate.py:311`, `orchestrator.py:153`) · renumbered from #96 2026-07-23 (dangling-hub-ref collision: #110/#128 **carried** `refs #96` in the HUB id space — closed upstream, audit 2026-07-20 A1 — which a local #96 assignment made silently resolve to this unrelated ticket). **Those carries were RESOLVED 2026-07-26**: both are now hub-qualified inside #84/#85, so no live ticket carries a bare `#96` and the hazard that forced this renumber is discharged
- [#115] [P3][S] **Self-referential verification has no home — the class that hid rules 1 and 7** (#97 F1 follow-up, ruled 2026-07-26 as F2 batch R1): a completeness test defined OVER the thing it checks has no denominator, so it covers whatever is present and **structurally cannot detect an omission**. Two live instances, both caught only by accident: `@pytest.mark.parametrize("leg", vc.RULES)` passed 41 green while two spec rules were absent from the registry, and #97 rule 14 leg (a) validates the ARCHITECTURE codemap against its own allowed-edge set (fixed by adding leg (b), source-derived). The F1 repair closed the **instance**; nothing closes the **class** · **RULED: two homes, not one** — **(a)** a `LESSONS.md` entry naming the class, its two instances, and the rule (a self-referential completeness check needs an EXTERNAL expected set, or a second leg derived from a different source); **(b)** a **test-collection assertion** — any `parametrize` over a module-level registry must have a companion external expected-set test, enforced mechanically at collection time rather than by reviewer memory · **RULED: explicitly NOT a checker rule for v1** — this is a property of the TEST SUITE, not a claim-vs-reality drift in the docs, and bolting it onto `validate_claims.py` would widen that script past its charter while the registry it would have to introspect is the same one it is checking · Done when: both homes exist — the LESSONS entry is appended (append-only, never edited) and the collection-time assertion fails on a seeded registry-parametrize with no companion test · **Design caution:** leg (b) must key on something more durable than a name match, or it becomes a naming convention enforced by regex — which is the same self-referential weakness one level up · refs `tests/test_validate_claims.py` (`SPEC_RULE_IDS`, the pattern to generalize), #97 gate (ii), #94/#95 (the adjacent test-trust items in this story)

---

## [E6] Council process & epistemic quality
> As the tool owner, I want the ADR-67 gated loop implemented, the synthesis rubric sharpened, and the panel's epistemics defended, so the Council runs deterministically and resists framing bias and false consensus.

### [S8] Build the ADR-67 downstream pieces and sharpen the rubric
So that `/council-question` generates + self-gates questions and synthesis quality is unambiguous.
- [#9] [P3][L] Implement ai-council's ADR-67 pieces: the `/council-question` template (one decision + options + constraints + prior-ADR context) and the question-quality gate · Done when: `/council-question` generates a templated question and the gate correctly passes/fails it · refs ADR-67 · DEFERRED — do NOT build before the canonical-baseline settles (mirrors `.dev-knowledge` #70) · NOTE: the deterministic `council.return_dir` I/O clause was moved out to #13 (baseline-INDEPENDENT)
- [#10] [P3][S] Refine the faithfulness criterion in `protocols/SYNTHESIS_QUALITY_RUBRIC.md` to clarify additive meta-analysis cases · Done when: the rubric wording disambiguates synthesizer cross-model synthesis vs raw-transcript content · refs N=1 scoring exercise
- [#11] [P3][S] Provide ai-council's two data points (cycle-1 + cycle-2 retrospective) for the "bilateral handshake = 1 round trip" codification owned by `.dev-knowledge` · Done when: the data points are handed to `.dev-knowledge/LESSONS.md` · refs cross-stream (codification lives in `.dev-knowledge`)
- [#55] [P3][M] Single-model / self-consistency baseline experiment (council-arch G5, the load-bearing gap → Open Question #1): the council never checks its synthesis against one strong model or a self-consistency run, so it cannot show it beats a matched-compute baseline in the verifiable regime · Done when: the baseline experiment design is delivered as **input to the T1 baseline-planning session** (the canonical-baseline settlement #9/E2 is gated on) — not built ahead of that session · refs `docs/audits/2026-06-16-council-architecture-vs-research-audit.md` G5 / Open Question #1, `arXiv:2502.08788`; baseline-gated, mirrors `.dev-knowledge` #70 and #9

### [S9] Defend the panel's epistemics
So that deadlocks resolve on evidence and consensus is genuine, not a framing artifact.
- [#18] [P3][M] Tool-grounded crux resolution [Rama 1]: when the panel deadlocks on a factual crux, resolve it by grounding in a tool/evidence lookup rather than more debate · Done when: a factual crux triggers a grounded lookup that feeds the next round · refs Rama 1, DRAFT-EPI-3 · **un-gated 2026-07-18** (Epic B / G3 event); planning deferred to a future session. Night council (2026-07-17) recommended option (c): a bounded crux-check step between rounds — see `docs/audits/2026-07-17-night-batch-empirical-e2e-audit.md` §2.2
- [#19] [P3][L] Active framing defense + false-consensus alarm [Rama 3, runtime-coupled]: detect and counter leading/asker-leaked framing at runtime and alarm when apparent consensus is an artifact of framing rather than genuine agreement · Done when: a framing-biased question is flagged + a false-consensus run raises an alarm · refs Rama 3, DRAFT-EPI-4 · runtime-coupled; **un-gated 2026-07-18** (Epic B / G3); planning deferred. Night council recommended option (b): a debate-time framing-challenge role, shadow-mode first — see night audit §2.2 · design predecessor: #103 (design commission — do not build #19 ahead of it)
- [#84] [P3][S] ai-council round-2 isolation audit — verify whether round-2 debaters see round-1 peer arguments; 2026 evidence: 58% sycophantic convergence in debate, 23.9% unanimous-wrong by round 3; confirm blind-vote isolation properties hold for our 2-round design · Done when: isolation properties confirmed or a remediation is recorded · refs hub #96 (CLOSED upstream 2026-07-20; **HUB** id space, not a local id — kept qualified rather than dropped so the provenance survives), ADR-03, research transcript 2026-06-06 · re-filed from the hub backlog 2026-07-08 (ADR-41 move, hub commit ea6217a); ADR-03 is the ai-council blind-voting ADR · **RENUMBERED #110 → #84 on 2026-07-26** into the local id space, discharging the reservation held since 2026-07-22; the hub id is retained here as provenance only (ref valid in-repo)
- [#49] [P3][M] EPI-2 identity-integrity six-field check (lane-epi): record `intent_match`, `intended_author`, `intent_source` (plus the companion fields) so a verdict's stated author/intent is verifiable against what the run actually produced — today only `synthesizer_model`/`error_class` observability exists · Done when: the verdict package carries the EPI-2 six-field identity-integrity block and a mismatch is detectable · refs `docs/intake/2026-07-06-lane-epi-functional-design.md` EPI-2, `src/ai_council/models.py` `synthesis_metrics`; sibling of the un-gated #18/#19
- [#58] [P3][S] Minority report emitted despite panel consensus (extractor over-fires): the 2026-07-18 CLI-vs-API smoke pair produced a minority report in **both** arms even though both arms reached a strong, independently-reasoned consensus (same verdict, aligned reasoning) — a spurious dissent artifact that misrepresents genuine agreement (the inverse of rubric item 2's hallucinated-consensus failure) · Done when: no minority report is emitted when the panel actually converged (dissent detection gates the minority-report emit) · refs `docs/audits/2026-07-18-smoke-pair-cli-vs-api-report.md` (both arms "+ minority report" despite "strong consensus"), minority-report emit path (#15 origin), verdict/output layer
- [#103] [P3][M] **#19 framing-defense / false-consensus alarm — DESIGN commission, not a build** (operator ruling 2026-07-25): #19 is an [L] runtime-coupled build with no design behind it, and the night council's option (b) — a debate-time framing-challenge role, shadow-mode first — is a one-line recommendation, not a specification. Commission the design: research the detection surface (what signal separates framing-artifact consensus from genuine agreement in a 2-round blind-vote design), then design the mechanism; the deliverable is a written design that **decomposes into filed tasks**. **Explicitly not a build — no runtime change lands under this id** · Done when: a dated design document exists and its implementation steps are filed as tasks under [S9] · refs #19 (the build this feeds), Rama 3 / DRAFT-EPI-4, `docs/audits/2026-07-17-night-batch-empirical-e2e-audit.md` §2.2 (option (b)), ADR-03 (blind voting — the design must not break it), #55 (the adjacent baseline experiment) · **NOT STARTED THIS WINDOW — 2026-07-26, ARCHITECT-OWED, recorded so it is owed rather than dropped.** This is a **browser-architect design commission, not CC work**: the deliverable is a design for the #19 framing-defense / false-consensus alarm, and CC building it would be CC commissioning itself. It was **displaced** by this window's record-repair and checker arcs — which were real work, but **scaffolding, not mission**. Third of the three standing axes (non-cognitive debate) and it moved zero this window, as did #55. Carrying the note so a future reader does not infer from a busy backlog that this axis advanced

---

## [E7] Record & governance hygiene
> As the tool owner, I want the decision record current and its lifecycle conventions ratified, so the canonical record never drifts from live state.

_[S12] Execute the GOV-1 currency pass — **delivered 2026-07-17** (#31: 15 lane-doc rulings → RULED, ADR-09/10 → Accepted, DRAFT-GOV-1 ratified as ADR-14, feature-work pause lifted; see JOURNAL + `docs/intake/archive/2026-07-17-gov1-rulings-register.md`). Theme backbone retained per the story-map._

### [S15] Mechanize the governance-record currency loop
So that record currency and lifecycle sync stop depending on hand-enforcement (the DRAFT-GOV-2 automation the GOV-1 pass deferred).
- [#50] [P3][S] DRAFT-GOV-2 `## Watches` protocol: add the ruled-but-unbuilt `## Watches` block to `BACKLOG.md` and seed watches W1–W3 (W1 pytest flake, W2 unpushed-main, W3 the seb block-cap) so recurring conditions are tracked, not remembered · Done when: the `## Watches` block exists with W1–W3 seeded and the validator accepts it · refs `docs/intake/2026-07-06-lane-gov-functional-design.md` DRAFT-GOV-2 (location ruled = BACKLOG); coordinate with the hub `validate-backlog` grammar before adding a new top-level section · **W4 candidate (2026-07-25):** JOURNAL drift toward stop-log — ADR-85's hard gate fires per session-STOP, not per substantive change (2 of #97 Unit 1's 5 branch commits were anchors only), so anchor volume tracks stop count rather than decision count; watch whether entries-per-substantive-change falls · see #102 for the numbered-list shape that adds a top-level section without a grammar change
- [#51] [P3][S] ADR-14 header↔index sync mechanization: ADR-14 mandates the ADR status line and the `docs/decisions/README.md` index stay in sync, but the invariant is hand-enforced today (this session bumped ADR-01 in both by hand) · Done when: a check (pre-commit or `check.ps1` step) fails when an ADR's status line and its README index row disagree · refs `docs/decisions/ADR-14-adr-lifecycle-states.md`, `docs/decisions/README.md`
- [#73] [P3][S] Review-runner convention is stale/ambiguous — decide the canonical invocation (findings only, no rewrite yet): **#33 and #44 both cited `codex-review.ps1 -Topic <x>` as the runner, but no `scripts/codex-review.ps1` exists in this repo** — every review this session (the #44 three-surface pass and the #33 verdict-package pass-3) ran through **direct `codex exec --sandbox read-only -c model=gpt-5.6-terra`** instead. The script is **not missing, it is mis-addressed**: a functional-looking `codex-review.ps1` (param block `-Topic`/`-DiffRange`/`-Focus`) lives at the **user level, `~/.claude/bin/codex-review.ps1`**, alongside the `/codex-review` command (`~/.claude/commands/`) that `docs/audits/2026-07-09-qa-lived-exercise.md` N5a witnessed resolving and running end-to-end. So the repo-relative citation is wrong, not the tool · Done when: it is **verified** whether the global script still runs against a current codex-cli (0.144.5 observed 2026-07-19; N5a witnessed it on 0.141.0), and the standing convention is **decided and recorded** — point at `/codex-review` + `~/.claude/bin/`, or at direct `codex exec`, or re-home a repo-local wrapper — with CLAUDE.md §8's "Code review: Codex via `/codex-review`" line reconciled to whichever wins · **HALF-VERIFIED 2026-07-20:** the global script **does** run end-to-end against codex-cli **0.144.5** — it produced `docs/audits/2026-07-19-codex-a1-failloud-adversarial.md` (8 High) this session, discharging the done-when's first clause; the *convention decision* remains open. **New defect found while running it, do not lose on decision:** its severity-summary heuristic (`~/.claude/bin/codex-review.ps1:270`) requires the label to be followed by `:` or `-`, but Codex now emits `### [HIGH] file:line — …`, so the console printed **High 0 over 8 real findings**. Measured across 68 codex audits in four repos: **8 reports understated High, 26 findings invisible**, earliest 2026-05-22. **No committed audit is wrong** — the count is `Write-Host` only and is never written to the file (`Set-Content` at `:262` precedes the count at `:272`), so every audit body is complete and this needs **no** amendment marker anywhere. Operator-owned fix. · **Findings only for now — no rewrite, and NO edit to `~/.claude/`** (core-invariant #6: global-infra edits are exception-with-ruling, never unilateral; if the answer is a global change, file it rather than make it) · refs `~/.claude/bin/codex-review.ps1`, `~/.claude/commands/codex-review.md`, CLAUDE.md §7/§8, `docs/audits/2026-07-09-qa-lived-exercise.md` N5a; surfaced by the 2026-07-19 #44/#33 review pass (JOURNAL 2026-07-19, caveat 1) · **RULED 2026-07-26 (F2 batch, R9) — point at the global script; do NOT re-home a repo-local wrapper.** Canonical invocation for a diff or full-audit review is `/codex-review` -> `~/.claude/bin/codex-review.ps1`; re-verified end-to-end **this session** (it produced `docs/audits/2026-07-26-codex-97-registry-repair.md` from the #97 F1 branch diff), the third independent witness after N5a (codex-cli 0.141.0) and the 2026-07-20 half-verification (0.144.5). A repo-local wrapper would duplicate a working global and re-create the drift this ticket records. The mis-addressed `scripts/codex-review.ps1` citations in #33/#44 were the error, not the tool; both are closed, so nothing needs correcting there. **Review-lane routing posture, ruled in the same breath because it lived only in `~/.claude` and JOURNAL and was therefore not a repo record:** `terra` = `gpt-5.6-terra`, pre-merge diff review, findings-only; `sol` = `gpt-5.6-sol`, adversarial passes and independent/blind derivations; `luna` = `gpt-5.6-luna`, drift fixtures. **Never a bare `gpt-5.6`** — an invalid identifier whose 400 is NOT evidence of an absent capability. Non-diff bounded prompts (design specs, blind derivations) go through direct `codex exec -m gpt-5.6-<lane> -s read-only`, NOT the review script, whose empty-diff guard would exit cleanly without ever invoking codex · **Done-when NARROWED to application:** CLAUDE.md section 8's review line carries the ruling above (runner + the three lanes + the direct-exec case). The verification half is discharged · **ROUTING CORRECTION, STANDING (operator, 2026-07-26) — a producer never reviews its own output:** any diff **terra** produced is reviewed by **sol**, never by terra. A producer reviewing its own work is not a pre-merge review, it is a self-check wearing the review's name, and it would quietly void the adversarial property the lane exists for. The converse holds too: terra remains the correct reviewer for **CC-produced** diffs (the usual case, and what ran this session). Fold this into the same CLAUDE section 8 edit as the ruling above — it is part of the routing posture, not a separate rule
- [#56] [P3][S] `docs/archive/` naming hazard: the folder named `archive` is actually the ADR-60 *pending-classification triage queue* (README policy = default-to-deletion after two reviews), NOT a preservation archive — the mismatch nearly mis-targeted the 2026-07-18 consolidation pass (preservation archives were instead created at `docs/audits/archive/` + `docs/intake/archive/`) · Done when: `docs/archive/`'s purpose is unambiguous at a glance — either rename to `docs/triage/` or add a prominent "NOT a preservation archive — deletion-tracked" banner to its README · refs `docs/archive/README.md` (ADR-60 amendment 2026-05-27); operator rider (c), 2026-07-18 consolidation · **FORK RULED 2026-07-26 (F2 batch) — BANNER, not rename.** Renaming to `docs/triage/` would invalidate every existing reference to `docs/archive/` (the ADR-60 amendment that created it, its own README policy, any audit citing the path) and would need an ADR-60 amendment to move a sanctioned taxonomy folder — all for a problem that is purely one of legibility. A banner costs one edit, zero references, and no ADR. It also keeps #68 out of it: `validate_docs_registry` keys on sanctioned taxonomy folder NAMES, so renaming one is precisely the change that would trip the guard · Done when: `docs/archive/README.md` opens with a banner reading, in substance, "NOT a preservation archive — this is the ADR-60 pending-classification triage queue; default disposition is DELETION after two reviews", above the existing policy text
- [#99] [P3][S] Triage the night-batch proposal set: `docs/audits/2026-07-22-pre-handoff-cleanup.md` **§2** holds the batch's evidence-backed PROPOSED items (leverage-ordered; §2.1 withdrawn by its amendment, `council-ask.ps1` deleted 2026-07-23, xdist declared + measured — the remainder: cache_invalidate/model_string keep-or-remove, KEY-SEALED.json relocation-at-unseal, duplication extractions, e2e-fixture optimization, lazy provider imports, fsck-arc prune decision, `.claude/worktrees/` nit, invariant-3 wording vs constructor defaults) — filed so a BACKLOG-only reader knows the un-triaged set EXISTS; individual filings are the triage session's scoped decisions, deliberately not made here · Done when: each §2 remainder is either filed as its own task, executed under explicit approval, or recorded declined — and this pointer task leaves · refs `docs/audits/2026-07-22-pre-handoff-cleanup.md` §2 + AMENDMENT, JOURNAL 2026-07-23 (fresh-architect gap 2)
- [#102] [P2][M] **Next-up ranked queue (5–10 items) at BACKLOG top, with a regroom trigger** (operator ruling 2026-07-25): the story-map gives structure and the P-tags give bands, but nothing gives **ORDER** — a reader facing 71 tasks across 7 themes gets no answer to "what next", and a P2 tag does not rank twenty P2s. **Not a redesign: the story-map and the P-tags stand; this adds order on top.** Two legs, and leg (ii) is what stops "re-groom at every close" from being prose: **(i)** `validate_backlog.py` checks every queue entry names a task that is open in this file — a queue citing a struck id is worse than no queue; **(ii)** an advisory session-close leg in `session_end_backpressure.py` that nudges when `BACKLOG.md` changed in the session's commits while the queue block did not. **Parse hazard, and the local workaround that clears it (ruled 2026-07-25):** `_THEME_RE` matches any `## ` heading, so a `## Next up` block parses as a **theme**, and any `- [#id]` line inside it parses as a **task** — which then hard-fails twice (`task not under a user story` AND `duplicate id`). **Write queue entries as a NUMBERED list (`1. #100 — short label`), never the `- [#id]` task shape** — that sidesteps both failures with no `_THEME_RE`/`_TASK_RE` change and therefore **no ADR-78 hub coordination** (`validate_backlog.py` is the hand-synced floor twin, so a grammar change would be a hub question; the numbered shape avoids making it one). Leg (i) then greps the ids out of the queue block and checks membership against the open-task set. The parser-learns-the-section option stays named as the alternative if the numbered shape proves insufficient, but the done-when is satisfiable locally · Done when: the queue block exists at BACKLOG top with 5–10 ranked open items in the numbered shape, `validate_backlog.py` still passes unchanged in grammar, leg (i) fails on a queue entry that is struck or absent, and leg (ii) fires on a BACKLOG-changed / queue-unchanged session and stays silent otherwise · refs `scripts/validate_backlog.py` (`_THEME_RE`/`_TASK_RE`/`validate`), `scripts/session_end_backpressure.py` `check_backlog_marker` (the advisory-leg pattern to copy, including its fire-once discipline), #50 (the other new-top-level-section item — same surface, sequence them)
- [#121] [P2][M] **A primary checkout has more than one writer, and nothing can tell them apart** (evidence assembled 2026-07-26; **fleet condition, filed here because the gate organ is local**): during the #116 arc the primary's reflog recorded **`HEAD@{6} 13:48:32 checkout: moving from fix/111-token-log-address to main`** — an entry no session command issued. It is a branch-pointer move, not a reset, so nothing was lost; it landed inside the `check.ps1` window; and **no in-repo cause exists** — no hook, script or pre-commit stanza executes a checkout (the two `git checkout` strings in `.claude/check_floor_hash.py` are message text), every git call in `tests/` is scoped with `-C <tmp repo>`, and the two `cwd=_REPO` scripts are invoked by neither `check.ps1` nor pytest. Cause is therefore **external to the repo** — harness rewind, a second terminal, or IDE git integration — and is not narrowable from inside it · **Not a one-off: documented precedent in the sibling repo.** The hub's `logs/OVERRIDES.md` records the ADR-85 session gate firing **twice on 2026-07-25** against *"a concurrent session's commits … which it checked out in the shared primary tree"*, and names the defect in its own words: session attribution is derived from *commits ahead of base* in a shared checkout, **so it cannot distinguish authors** and re-arms against the wrong session on every concurrent commit. That is why the second firing could not be repaired honestly — the only literal repair was to claim another session's commits as this one's, which is false authorship · **THE CLASS, stated explicitly:** `commits-ahead-of-base` is a **value published without the predicate that produces it** — it is used as a proxy for "this session's work" while its actual predicate is "commits on this branch not on the base, by anyone". The two coincide only while a checkout has one writer, and that precondition is nowhere asserted. This is exactly the LESSONS 2026-07-26 class, and its fifth instance · **Consequence for process, not just for the gate:** serial integration through the primary is only serial **if the primary has one writer**. Every "record HEAD → act → verify first-parent" bracket is a *detector* for this, not a fix — the 2026-07-26 Stream-A merge used one and it held, but a bracket that holds proves only that nothing moved *that time* · Done when: **either** session attribution stops being derived from commits-ahead-of-base (an authorship-bearing signal — session id on the commit, a per-session ref, or a writer lock on the checkout), **or** the single-writer precondition is asserted mechanically and the gate degrades loudly when it does not hold — and in both cases the ADR-85 gate stops being repairable only by false authorship · **EVIDENCE UPGRADE 2026-07-26 (config sweep) — one of the three candidate causes now has DEMONSTRATED write access to this checkout.** This ticket named three candidates for the unattributed `HEAD@{6}` checkout: harness rewind, a second terminal, or IDE git integration. A full `git config --local --list` sweep found **exactly one** local-only value on this checkout with an external writer: **`branch.main.vscode-merge-base=origin/main`**, written by the VS Code git integration — every other key is stock plumbing, identity or remote config. **State this precisely: it narrows the candidate set, it does not prove the checkout event.** What is demonstrated is that the IDE integration writes to this repository's local config unprompted; what is NOT demonstrated is that it performed the branch switch. A writer with proven access is a stronger candidate than two with none, and the value is otherwise harmless (a UI diff hint, not load-bearing) — it is cited for its WRITER, not its content · refs `scripts/session_end_backpressure.py` (the local gate organ), `.dev-knowledge/logs/OVERRIDES.md` (two firings, 2026-07-25), ai-council reflog `HEAD@{6}` 2026-07-26, LESSONS 2026-07-26 (the class), **hub #423** (the integration sequence is unmechanized prose — its own evidence includes a live-session enumeration that gave a false LIVE from a subset scan, the same cannot-tell-sessions-apart family; this ticket is the attribution half, #423 the sequencing half, neither subsumes the other) · **needs a hub twin** — the condition is fleet-level while this ticket is scoped to the consumer that witnessed it · **BRACKET CORRECTED 2026-07-26 (operator ruling), after it produced a false positive on its first real use:** the one-condition form — record HEAD, act, assert first-parent equals the recorded HEAD — **cannot distinguish "the hazard occurred" from "the operation never ran".** A failed `git merge` leaves HEAD unchanged, so `git log -1 --format=%P` reads the OLD commit's parent, which differs from the recorded HEAD and reports FAILED. Witnessed exactly so: `git merge -F -` errored (`could not read file '-'`), the merge never ran, and the bracket cried mutation. **Correct form is TWO conditions, in order: (1) assert HEAD CHANGED, (2) assert the new first-parent equals the recorded HEAD.** Condition (1) separates no-op from hazard; condition (2) is the hazard test. A bracket that conflates them trains the operator to discount it, which is worse than having none · refs the `git merge -F -` gotcha (#122)
- [#122] [P3][S] **`git merge -F -` is not supported and fails silently-ish — cost time twice, named as unfiled in the last handoff supplement** (filed 2026-07-26): `git commit -F -` reads the message from **stdin**, and the same habit applied to `git merge` errors with `error: could not read file '-'` — **the merge does not run**, but the surrounding script continues, so the failure surfaces later and somewhere else. On 2026-07-26 it fired inside a bracketed merge and the bracket then reported a false primary mutation (see #121), turning a one-line syntax error into an apparent fleet-safety incident · **Why it recurs:** `-F -` is correct for `git commit` and for `git tag -a`, so the wrong generalisation is the natural one, and heredoc-into-git is the standard shape in this repo's agent workflows · Done when: the gotcha is in `~/.claude/skills/gotchas/gotchas.md` with trigger (any `git merge`/`git tag` message passed by heredoc), symptom (`could not read file '-'`, operation silently skipped) and fix (write the message to a temp file and pass its path; or `-m` for a single line) · **Global-infra note:** the gotchas file is under `~/.claude/`, so per core-invariant 6 this is filed here and applied there only with an explicit operator ruling for that edit · refs #121 (the bracket that mis-reported because of it), the 2026-07-25 handoff supplement (named unfiled)
- [#123] [P2][M] **The whole gate runs on the SYSTEM interpreter, not the repo venv — repoint first, uninstall second** (root cause of the worktree wrong-tree import, 2026-07-26; **WORKFLOW change, not a venv build**): `scripts/check.ps1` calls bare `pytest`, `mypy` and `python`, and the system `Python312` carries **both** pytest and an editable `ai_council` install pointing at the primary checkout. So every `py -m pytest` and every `check.ps1` run in this repo has executed on the system interpreter, and `import ai_council` resolves to the primary `src/` **from any working directory** — which is why a worktree silently tested the primary's code until the root `conftest.py` guard was added · **The primary already HAS a correct venv** (`.venv/Lib/site-packages/__editable__.ai_council-1.0.0.pth`), so this is not a build; the defect is that nothing *uses* it · **ORDER IS LOAD-BEARING — repoint, THEN uninstall.** Uninstalling the system-level package first breaks every bare invocation at once (check.ps1's three tools, any hook needing the package) with no fallback · Done when: `check.ps1` and every hook that needs the package invoke `.venv\Scripts\python.exe` explicitly (`-m pytest`, `-m mypy`, `-m ruff`), a run proves the gate green through the venv, and ONLY then `pip uninstall ai-council` on the system interpreter, re-verified by the `conftest.py` guard failing loudly under bare `python` and passing under the venv · **MACHINE-LEVEL, operator's call** — the system site-packages is outside this repo, and other repos may rely on it · **Do NOT execute inside another arc**: it changes how every test run in the repo resolves, so it needs its own window and its own green gate · **BLOCKED BY #124 (operator ruling 2026-07-26) — order is not a preference, it is a correctness constraint.** This ticket repoints `check.ps1` and the hooks at `.venv`. If that venv is installed from the **declared floors**, the gate goes **RED on the day this lands** — mypy 2.3.0 + google-genai 2.14.0 fail `mypy src/` with 3 errors, which is precisely what #124 describes. Executed first, #123 does not surface a latent problem gracefully; it **breaks the green gate** and the breakage looks like #123's own regression, so the diagnosis starts in the wrong place. #124 must land first so the venv this ticket points at is one that can be green · refs `scripts/check.ps1`, `conftest.py` (the guard that made it visible), `.claude/settings.json` hooks, #121, **#124 (blocker)**

### [S18] Build the claim-vs-reality checker
So that the doc-drift class the 2026-07-22 night batch found by hand (and every prior audit re-derived from scratch) is caught mechanically on every run, with each rule traced to a real drift it would have caught (spec source: `docs/audits/2026-07-22-pre-handoff-cleanup.md` §3, absorbed 2026-07-23).
- [#97] [P2][M] Claim-vs-reality checker v1 — fourteen rules (twelve batch-derived + two absorbed 2026-07-23) as a read-only script. **PARTIALLY DELIVERED — Unit 1 merged `b94e8d1` (2026-07-24):** `scripts/validate_claims.py` implements rules **2, 3, 4, 8**, and **rule 12 is structural rather than a leg** — realized in the `Finding` model's evidence-argv field plus its `shlex` round-trip invariant, so every emitted finding carries a re-runnable command by construction. Wired as a **non-blocking findings section** of `check.ps1` (report-only, exit deliberately swallowed at the call site — NOT a pre-commit gate, per #73's runner-convention ruling). **OUTSTANDING IS NINE — and as of the 2026-07-26 F1 repair all nine are REGISTERED STUBS** (1/5/6/7/9/10/11/13/14, reporting SKIP). The count is unchanged; the composition is not. Rules **1 and 7 were ABSENT FROM THE `RULES` REGISTRY ENTIRELY** until then (discovered 2026-07-25 — no leg, no `_unit2_stub` row, no `rule_id=`, and the report's KNOWN LIMITATIONS named only the seven stubs, so **no run disclosed them**: a reader saw 4 implemented + 7 skipped with no denominator to compare against). Both are now registered, and the report carries a **computed** coverage block (implemented 4 / stubbed 9 / structural 1 / absent 0, TOTAL 14 of 14) whose `absent` count is **live** — it goes non-zero the moment a leg is dropped, proven by a seeded omission. **Their earned-by drifts are still unmonitored** — rule 1's `boost`/`crux_check` absence from all three ARCHITECTURE tables, and rule 7's ADR-11 stamp predating its own amendment — because registering a rule **discloses** it, it does not **check** it. The fourteen rules: **(1)** module-table completeness — every `src/ai_council/*.py` module appears in ARCHITECTURE's codemap/responsibility/layer tables *(caught: `boost`, `crux_check` absent from all three)*; **(2)** path existence — backtick-quoted repo paths in canonical docs resolve on disk, hub-qualified (`.dev-knowledge/...`) and historical sections (§12, JOURNAL, dated audits) allowlisted *(caught: `src/ai_council/config/`, GUIDE→README.md)*; **(3)** hook-roster parity — set-equality between `.pre-commit-config.yaml` ids and the ARCHITECTURE/CLAUDE §9 rosters *(caught: 10 vs 12)*; **(4)** ADR-roster parity — ADR files on disk == CLAUDE §11 == ARCHITECTURE Governing-ADRs == `docs/decisions/README.md` index *(caught: ADR-13 unrostered)*; **(5)** config-claim parity — doc claims of defaults anchored to `settings.yaml` keys, **adjudicated against EFFECTIVE flag-resolution behaviour, never raw key values alone** — the panel-default "conflict" was a rule-5 false positive: `default_panel` is deliberately the lite set and `cli.py:689/812` (`use_full_panel or not lite`) makes full_panel the effective default exactly as ADR-02's Implementation section documents *(caught: o3/o4 stale model names; false-positived: panel default — hence this clause)*, and **read the audited document's own explanatory sections before reporting a contradiction with it** — the panel-default false positive existed because Key Design Decisions already explained the indirection in the very file being audited; **(6)** CLI-surface parity — `@main.command` registrations == documented subcommands *(caught: `boost`)*; **(7)** stamp honesty — `last_reviewed`/`Deployment-Status` ≥ last substantive commit, extended to ADR deployment stamps which sit outside the `canonical_freshness` A2 gate *(caught: ADR-11 stamp)*; **(8)** SHA-citation resolution — word-bounded hex ≥7 chars in JOURNAL/BACKLOG resolves in-repo or sits in a hub-qualified sentence *(kills the `feedbac` false-positive class)*; **(9)** durations regression gate — flag any unit test over threshold *(caught: the 30.07s dead-patch test)*; **(10)** dep parity — venv-installed vs pyproject-declared *(caught: pytest-xdist)*; **(11)** invariants as grep-able assertions — each ARCHITECTURE invariant carries a mechanical verify *(caught: invariants 1/5 false as written)*; **(12) process rule, not decoration** — every finding the checker emits carries a re-runnable evidence command; the batch's own scanner produced four false positives (`check_floor_hash.py` "missing", CLAUDE:239, `feedbac`-as-SHA, the rule-5 panel-default misread) and a checker that reports without evidence commands recreates the audits-never-absorbed failure mode; **(13)** ticket-reference resolution — every `#<id>` cited in canonical docs (ARCHITECTURE, CLAUDE, VISION, protocols/) resolves to a live BACKLOG item, a reserved id, or carries a historical marker *(earned by: the #84/#85 collision, the dangling hub `refs #96`, the stale "pending #2" clause — and by the "#97 rule 14" forward reference in the 2026-07-23 architect repair, which this rule caught in its own author's edit)*; **(14)** layer-edge conformance, **two legs** — **(a)** every edge in the ARCHITECTURE codemap falls inside the declared allowed layer-edge set or appears as a named open case; **(b)** every real import between modules in `src/` appears in the codemap — map completeness against source, the leg that makes this a mechanism rather than an instruction: leg (a) alone validates the hand-maintained map against itself, so an illegal import the map omits passes the gate — the exact case the rule exists to catch *(earned by: `cli -> boost` going unnoticed for a day; leg (b) validated LIVE 2026-07-23 the moment it was specified — a source grep of `cli.py` found ~11 real module imports absent from the codemap, incl. `cli -> inbox` (interface→interface) and `cli -> config` (interface→foundation), neither in the allowed set; ARCHITECTURE's "only edge that skips a layer" claim and its allowed set are map-derived and need a follow-up edit once ruled)* · Done when: the script runs read-only over the repo, reports PASS/FAIL per rule with an evidence command per finding, and a seeded violation of each rule is demonstrably caught — **plus two explicit gates added 2026-07-25, because the first three clauses are ALREADY TRUE for the implemented subset and a reader checking this done-when against a live run could otherwise reasonably strike the line today: (i)** all **fourteen** rules are **registered**, with rule 12's structural realization in the `Finding` model recorded as the one sanctioned exemption; **(ii)** a **registry-completeness test asserts the `RULES` id set against an EXPLICIT expected set** — the existing `@pytest.mark.parametrize("leg", vc.RULES)` iterates the registry, so it covers whatever is present and **structurally cannot detect an omission** (41 new tests passed green while rules 1 and 7 were missing, and would continue to); an **external denominator** is the only thing that closes that class — **GATES (i) AND (ii) MET 2026-07-26** (F1 repair): rules 1 and 7 registered as Unit-2 stubs, and `tests/test_validate_claims.py::test_registry_covers_the_full_spec_id_set` asserts a literal `{1..14}` against the `RULES` id set minus the documented rule-12 exemption. Landed **RED first** (`RULES drifted from the #97 spec: missing [1, 7], unexpected []`) and re-proven afterwards by a **seeded omission** — dropping the rule-7 row yields `missing [7]` from the test and `absent      (1): 7` from the live report. The expected set was **independently re-derived from this ticket's own inline spec by sol** (`gpt-5.6-sol`) with no sight of `scripts/validate_claims.py` or the test file, agreeing at `{1..14}`, contiguous, rule 12 identified as the non-leg exemption — two independent derivations agreeing is what makes the denominator evidence rather than an echo. **Residual disclosed and filed as #114:** `_SPEC_RULE_IDS` is hand-copied from this line, so a change to the SPEC itself is undetectable; named in the report's KNOWN LIMITATIONS rather than left silent. **The line stays OPEN on the remaining clause** — a seeded violation of *each* rule needs the nine stubs implemented · **#104 inherits this denominator:** its "one clean window per rule" trigger **must not gate before the registry is complete** — gating on an 11-entry registry would declare the checker done at 11 of 14 · refs `docs/audits/2026-07-22-pre-handoff-cleanup.md` §3 (+ its 2026-07-23 amendment on rule 5), `docs/audits/2026-07-20-night-code-audit-opus.md`, #73 (runner-convention), #51 (rule-4's ADR-index half — subsumes or coordinates) · **THE SEVEN REMAINING UNIT-2 STUBS ARE HELD BY ARCHITECT RULING (2026-07-26)** — rules **5/6/7/9/10/11/13** stay REGISTERED and reporting SKIP, and are **not scheduled**. **CORRECTED 2026-07-26: the held list first read `5/6/9/10/11/13/14`, which was wrong in both directions** — it omitted rule **7** and wrongly held rule **14**, and rule 14 cannot be both held and next while the JOURNAL records the next unit as "rule 1 + rule 14 leg (b)". **SCHEDULED NEXT UNIT: rule 1 + rule 14 leg (b), as ONE unit**, because they share the definition of a module and splitting them would let each validate against its own reading. **Pinned predicate:** a MODULE is an entry in ARCHITECTURE's Modules table — name from column 1, path from column 3; an EDGE is any import whose target is an enumerated module **regardless of location**; `config` is the forcing case, enumerated at repo root rather than under `src/ai_council/`, so a `src/`-scoped definition would silently drop `cli -> config`. Seven held + rule 1 and rule 14 scheduled + four implemented + rule 12 structural = 14, which is the arithmetic the earlier list failed. Reason: **diminishing value against the mission**. The checker already earns its keep on the drift classes that actually bit (paths, hook roster, ADR roster, SHA reachability), and the remaining seven are speculative against a repo whose canonical surface is now clean; building them would spend the mission's scarcest resource on rules no observed drift is asking for. **Revisit trigger: when a drift the held rule would have caught actually bites** — then build that ONE rule, earned by the incident, exactly as rules 1–14 were each earned. **HELD, NOT FORGOTTEN AND NOT SILENTLY DROPPED:** they remain registered, so every run still prints them as SKIP inside the computed coverage block (`stubbed (9)`, `TOTAL 14 of 14`) — the denominator keeps disclosing them, which is the entire reason registration was insisted on rather than deletion. A dropped rule would vanish from the report; a held one keeps saying "not checked". **Consequence for #104:** its per-rule promotion criteria apply to the four implemented rules only; a SKIP is not a clean window and can never earn one · **#119, #120 and #105 also stay FILED and UNSCHEDULED** under the same ruling — real defects, reproduced first-hand, with no current claim on the queue
- [#104] [P2][S] **Promote the claim-checker from report to gate** (#97 Unit 1 follow-up, merge `b94e8d1`): `check.ps1` runs `validate_claims.py` and deliberately swallows its exit — the script already exits 1 on findings and ≥2 on error, so promotion is **one line at the call site** (propagate `$claimsExit`), not a code change. **Trigger — RULED 2026-07-26 (F2 batch, R5), REPLACING the earlier prose "one clean window per rule":** the checker **stays report-only** until a rule meets **all three** of — **(i)** an **external denominator test** covers it: the rule id is asserted against a literal expected set maintained OUTSIDE the registry (landed registry-wide 2026-07-26, #97 gate (ii)); **(ii)** the rule returns **identical results on a fresh clone at the same commit** — #116 is the standing counter-example, so R2 fails this today by construction; **(iii)** **zero distinct defects, FOR THAT RULE, on every first-parent commit of `main` across >=5 consecutive commits** — keyed on the defect SUBJECT, not raw finding instances. **AMENDED 2026-07-26: criterion (iii) is PER-RULE, and promotion is per-rule rather than all-or-nothing.** Read report-wide it produces an absurdity — a newly implemented rule finding a REAL defect would reset the promotion clock for every already-clean rule, punishing the checker for working and creating a standing incentive not to add rules. Each rule earns and holds its own window; the gate arms per rule at the `check.ps1` call site. **A rule's clock restarts when its own implementation changes**, because prior clean runs are then evidence about a different rule. "One clean window" was never operable: it named no window length, no defect unit, and no checkout, so no run could ever satisfy or fail it · **UNBLOCKED 2026-07-26 — both blockers discharged, and #104 is now PROMOTABLE.** **#108 closed:** R2 adjudicates a path claim in its own sentence via a shared pure predicate (negated / externally-attributed / hypothetical), so the context-blindness false positives are gone with **no** allowlist entry added. **#116 closed:** R2 resolves against the **git-tracked tree** under declared bases instead of the disk, so criterion (ii) is met — verified by running the checker on this checkout AND on a fresh clone at the same commit, with `output/` and `logs/` present on one and absent on the other, both reporting `pass 4 | FINDINGS 0`. **#105 was struck as a blocker on 2026-07-26 (F2 R2)** and remains so. **What is NOT yet done — promotable is not promoted. Measured 2026-07-26** by running EACH first-parent commit's OWN checker in a throwaway clone: **rule 3 = 11 consecutive PASS, rule 4 = 11** (the checker's entire life — `b94e8d1` is Unit 1 and the commit before it has no checker), both with implementations unchanged throughout, so **both ALREADY clear criterion (iii) at >=5**. **Rule 8 = 11 raw but its clock RESTARTS at `e45c193`** (#108 added hub-attribution classification, so its behaviour changed) → **4 consecutive**, one short. **Rule 2 = 2, and restarts again now** — ADR-15's resolution model changed it, so its prior runs are evidence about a different rule. Arming a rule before its own window elapses would assert a run of evidence that does not exist · Done when: the exit guard is armed at the `check.ps1` call site for the rules that met all three criteria, and the promotion criterion is recorded where the next session will find it · Done when: the exit guard is armed at the `check.ps1` call site for the rules that met the trigger, and the promotion criterion (which rules gate, on which count) is recorded where the next session will find it · refs `scripts/check.ps1:16-24`, `scripts/validate_claims.py` `exit_code`, #105 + #108 (the blockers), #97 · **CRITERION (iv), ADDED 2026-07-26 — for any rule promoted to gating, `anchor-missing` is a **FAILURE**, not a WARN.** As the harness stands, a rule locates its subject by matching a section heading in the very document it audits (`_VALIDATOR_SECTION` for rule 3, `_ADR_SECTION` for rule 4); reword that heading and the leg returns `anchor-missing`, which does not fail — so **the artifact can silently disable its own check**. Tolerable while the checker only reports; inadmissible the moment it gates, because the un-gating is invisible in the exit code. The anchor-missing WARN semantics stay for non-gating rules — it remains the precision lever that keeps a reworded doc from synthesising a false finding — so (iv) is a promotion-time status flip, not a harness-wide change · **CLOCK RESET, recorded 2026-07-26:** all four implemented rules (2/3/4/8) restart their (iii) windows at the harness commit-tree fix, since that change alters every leg's inputs. **The measured "rule 3 = 11, rule 4 = 11" was never credit toward promotion** — (iii) is conjunctive with (ii), and (ii) was never met by either rule: both were empirically shown on 2026-07-26 to flip verdict on working state (a dirty `.pre-commit-config.yaml` fails rule 3; a single untracked `docs/decisions/ADR-99-scratch.md` fails rule 4), so the 11 was 11 clean runs of a rule that could not satisfy the criterion it was being counted against
- [#105] [P2][S] **The checker reports findings but not distinct defects** (#97 Unit 1 follow-up): `format_report`'s SUMMARY is `FINDINGS n across m rules` — a single count, keyed per **citation site**. The first run of record shows **6 R8 findings from 4 dangling SHAs** (`6a44191` at JOURNAL:1947 and :1949; `26192dd` at :26 and :788), so the headline overstates the repair work by half and moves when a doc is re-cited rather than when the repo changes. **Worse, and this is why it blocks #104:** JOURNAL:26 is the session entry the Unit-1 merge had just integrated — a **new** entry re-cited a dead SHA (as the rule-12 evidence that the rule works), so the defect **self-replicates**; a count that grows with every citation cannot be gated to zero · Done when: the summary reports **both** counts — findings and distinct defects keyed on the defect subject (R8 = the SHA, R2 = the path) — and the per-rule finding list groups by subject · refs `scripts/validate_claims.py` `format_report`/`_sorted_findings` (a `Finding` carries `location` but no subject field — that is the change), #104 (blocked by this), #112 (the four SHAs themselves — **CLOSED 2026-07-26**, historical: the four dangling commits were made reachable from pushed `archive/cited-*` tags and `gc.auto` was restored to its git default, merge `e311e55`; this citation resolves as history, not as a live id) · **NO LONGER A #104 BLOCKER — ruled 2026-07-26 (F2 batch, R2):** the blocking argument was self-replication (a new JOURNAL entry re-citing a dead SHA grows the count faster than repair), and that premise died when **R8 went to zero** — the four SHAs are reachable from pushed `archive/cited-*` tags (#112 closed). The count is now stable, so a gate can reach it. **Stays OPEN on its merits:** #104 criterion (iii) is stated in **distinct defects**, so this ticket is what makes that criterion measurable rather than what blocks it
- [#114] [P3][S] **The checker's spec id set is hand-copied from #97, so a change to the SPEC itself is undetectable** (#97 F1 residual, disclosed 2026-07-26 — **disclosed in the report, not silent**): `_SPEC_RULE_IDS` in `scripts/validate_claims.py` and `SPEC_RULE_IDS` in `tests/test_validate_claims.py` are two hand-maintained literals of `{1..14}`, bound to each other by `test_spec_constants_match_the_checkers_own`. That binding catches drift **between the two literals**; it cannot catch drift **from #97**. Amend the spec to a fifteenth rule and both literals stay at fourteen, every test stays green, and the report keeps printing `TOTAL 14 of 14 accounted for` — the checker understates its own denominator exactly as it did while rules 1 and 7 sat absent, one level up. This is the F1 repair's known ceiling: F1 closed the registry-vs-spec gap and left the spec-vs-ticket gap open on purpose, disclosing it in KNOWN LIMITATIONS with this id · **Does NOT block #104** — the registry is complete against the spec as written, which is what #104's denominator clause required; this is a further-up-the-stack check, not a gating precondition · Done when: a leg or test parses #97's own inline rule enumeration out of `BACKLOG.md` and asserts it equals `_SPEC_RULE_IDS`, with a seeded fifteenth rule in a fixture demonstrably caught · **Fail-loud constraint (do not skip):** the parse target is a single ~30KB BACKLOG line of dense prose whose rules are inline `**(n)** name` spans, not list items — a brittle regex here trades a *disclosed* gap for an *undisclosed false pass*, which is strictly worse, so a parse yielding anything other than fourteen ids must be a **CHECKER ERROR (exit >=2)**, never a silent pass · refs `scripts/validate_claims.py` `_SPEC_RULE_IDS` + its KNOWN LIMITATIONS block, `tests/test_validate_claims.py` `SPEC_RULE_IDS`, #97 gate (ii), #105 (the other open #104 blocker)
- [#126] [P2][S] **Four gating validators assert nothing — their exit 0 is silence, not a verdict** (found 2026-07-26 in the session-close gate sweep): `canonical_freshness_gate.py`, `validate_docs_registry.py`, `validate_sealed_keys.py` and `validate_audit_casing.py` emit **no output at all** — bare or with `--all`, and none of the four parses `sys.argv`/`argparse` — so a clean run and a run that examined **zero items** are byte-identical to a reader. Their exit 0 is therefore **materially weaker evidence** than `validate_backlog.py`'s ("7 themes, 15 stories, 77 tasks") or `validate_claims.py`'s SUMMARY line, both of which state what was checked and how much · **This is the shape CLAUDE §10 already warns about** — "running validators with no args: vacuous pass" — and it is **LESSONS 2026-07-26** exactly: a green published without the predicate that produced it. Absence of output was read as absence of drift in this very sweep, and only the anti-pattern note prevented scoring it as clean evidence · **Load-bearing, not cosmetic:** all four are armed `pre-commit` gates, so a silently-degraded one (a glob that matches nothing after a directory move, a registry that fails to parse) passes every commit while checking nothing, and nothing in the output would ever say so · Done when: each of the four prints a **positive assertion** naming what it checked and **how many items** it checked (e.g. "validate_audit_casing: OK (N audit filenames checked)"), so a silent run is distinguishable from a run that checked nothing — and a seeded zero-item run is demonstrably distinguishable from a clean one · **Do not satisfy this by printing a constant banner** — the count is the whole point; a fixed "OK" reproduces the defect with extra characters · **Id note:** the HUB id space holds its own #126 (backpressure-loop pattern evaluation); grepped before assigning and **nothing in ai-council references it**, so there is no dangling-reference hazard — same disposition as the #116/#117 hub/local overlap recorded 2026-07-26 · refs `scripts/canonical_freshness_gate.py`, `scripts/validate_docs_registry.py`, `scripts/validate_sealed_keys.py`, `scripts/validate_audit_casing.py`, `.pre-commit-config.yaml` (all four armed), CLAUDE.md §10 (the anti-pattern), LESSONS 2026-07-26, #125 (the sibling case — a gate whose report overstated what it read)

---

**About this file** — ADR-66 story-map (Big Picture → Theme → User Story → Task), migrated
2026-06-02 from the ADR-41/47 stream schema per ADR-38 A6 (canonical backlog form, all
repos). Themes carry a stable `[E<n>]` epic id; stories are human (goal + `So that`) and
carry a stable `[S<n>]` id (#281/#286); tasks carry `[#id] [P][size] · Done when · refs`.
Structure is checked by the `validate-backlog` pre-commit gate (ADR-66). Done tasks **leave** (ADR-65); git is the
implementation record. Conformance is checked read-only by `.dev-knowledge/scripts/audit.py`.

**Id reservations (do not assign):** **#84 / #85 — RESERVATION DISCHARGED 2026-07-26, ids now IN USE.** The carried hub-id tasks were renumbered **#110 → #84** and **#128 → #85** (ADR-41 re-file 2026-07-08 had kept the hub ids; this landed them in the local id space). Do not re-assign #84/#85 — they are live tasks, not free ids. The 2026-07-22 reservation was recorded after an unrecorded one collided with two new filings (renumbered to #86/#87 same day). **#96 — RESERVATION ALSO DISCHARGED 2026-07-26, and #96 is FREE.** It was reserved as a **dangling-reference target** because #110/#128 carried `refs #96` in the HUB id space (closed upstream; audit 2026-07-20 A1), so a local #96 would silently capture them — it did once, 2026-07-23, and was renumbered to #98 same day. Both carries are now **hub-qualified inside #84/#85**, so no live task line holds a bare `#96`; the remaining mentions (#97's rule-13 justification, #98's renumber provenance, and this log) are historical prose that names the HUB id space explicitly. The stated cause is gone, so the reservation goes with it rather than outliving it as a permanent record of a temporary hazard. **Rule extended 2026-07-23:** this note covers BOTH planned renumber destinations AND ids that existing text dangling-references — before assigning any new id, grep for it first. **#107** is reserved as a **dangling-reference target** (added 2026-07-25, caught by that grep): `council_inbox/archive/2026-07-08T1817_2026-07-07_DK-EXTRACT_prompt-standards.md:275`/`:381` cite `#107` in the **HUB** id space (the native-worktree convention) — a local #107 would silently capture those refs. This is the **THIRD instance** of hub/local id-space collision (`refs #96`; the #110/#128 hub-id carries; now #107), and this repo is already paying for the first via the pending #110→#84 / #128→#85 renumber. **Reactive reservation per collision is the patch; #97 rule 13 (ticket-reference resolution, Unit 2) is the fix** — its BACKLOG live-item/reservations parser is what makes manual collision-hunting unnecessary. Whoever builds rule 13: this is a concrete case to test against — a hub-space id cited in `council_inbox/archive/` that a local assignment would silently capture.

**Grooming log:** 2026-05-12 (stream-format seed) · 2026-06-02 (story-map migration, all 11 items preserved) · 2026-07-02 (6-segment backbone reorganization: 4 themes → 6 lettered segments A–F; #12–#19 added; #9 re-sliced (return_dir I/O → #13); #20 filed under the provider-reliability segment (C) — pre-existing mypy drift surfaced during Unit 1; all 11 prior items preserved; #12 completed + struck (ADR-65) once the protocols/ surface landed — git carries the record) · 2026-07-02 (invocation-surface segment (A) output subsystem shipped: #13 return_dir routing, #14 double-council fix, #15 minority report closed + struck per ADR-65 — commits bfc268f/53ad525/f1a4b74; that segment's story fully delivered, backbone header retained) · 2026-07-08 (Wave-1 onboarding: renamed the thematic backbone from the retired lettered scheme to named themes per ADR-99 clause A; adopted stable `[S<n>]` story ids per #281/#286; re-filed #110 + #128 from the hub backlog per the ADR-41 move (hub commit ea6217a); all task ids preserved) · 2026-07-11 (filed #21 under S3 — stale `test_full_debate_pipeline` integration test surfaced during the #326 arc; next-free local id after #20) · 2026-07-13 (content-parity D1: added `[E1]`–`[E6]` epic ids to the 6 theme headers + the epic-ids backbone line, and wired the ADR-66 `validate-backlog` gate — ADR-78 floor twin, hub audit `2026-07-13-technical-content-parity-inventory.md`; stories/tasks unchanged, all ids preserved) · 2026-07-16 (plan-of-record reconciliation: filed #22–#31; new theme [E7] + stories [S10]–[S12] — additive, all prior ids preserved; #16 gained an ADR-12/pre-work clause; the phase→task map lives in `docs/intake/archive/2026-07-16-plan-of-record.md`) · 2026-07-17 (GOV-1 consolidation, gate G1→G2: 15 lane-doc §6 rulings recorded → RULED in `docs/intake/archive/2026-07-17-gov1-rulings-register.md`; **FORK_RULING(a)** — #1 absorbed into #24 (#24 is the authoritative full-corpus evidence path for #2's Branch A/B decision; #2 re-pointed #1→#24); ADR-09/10 → Accepted + DRAFT-GOV-1 ratified as ADR-14; feature-work pause lifted). · 2026-07-17 (**#28 CLOSED** — F3 grok cost lane: operator ran `grok login` (subscription OAuth); repo re-probe with scoped env-key shielding witnessed the subscription lane ("logged in with grok.com"; `~/.grok/auth.json` present) — done-when met, disposition amended in `docs/audits/2026-07-17-f3-grok-cost-lane-disposition.md`; grok seat still deferred to #27 parity, pins grok-4.5, scope-shields `XAI_API_KEY` per call). · 2026-07-17 (**#16 CLOSED** — CLI seats claude+codex: A1 template-method base + A3 one-classifier/retry (MERGE 1) then CLI adapters + seat-router admission gate + same-seat API fallback + `seats[]` sidecar + backend axis (MERGE 2); witnessed live — both CLI seats participate end-to-end + harness-induced `fallback_events[cause=process-error]`; CLI calls $0-cost. **Filed #32** (doctor-v2 CLI-fleet auth-lane check) as the explicitly-waived terra-gate residual per architect ruling). Next quarterly: 2026-10-01. · 2026-07-17 (**#25 struck** — `council doctor` v1 delivered at merge `6e0782e` (DRAFT-DOC-1 + A2 `@click.group`; terra-clean); done-task leaves per ADR-65, git carries the record. Pre-step of the #26 verdict-package arc; [S11] retains open #32.) · 2026-07-17 (**#26 CLOSED** — verdict package per DRAFT-INT-1: `save_verdict_package` sibling of `save_to_file` (pre-work A4 decompose + B3 `_ts()` helper) emitting `council-verdict-<ts>-<mode>-<slug>.json` to every destination with the full 14-field set; human-readable mirror block folded into `_build_header`; consumes `seats[]`/`synthesis` by reference via the shared `_seat_payload`. Witnessed live on shipping code (merge below): schema-valid package, deterministic `<ts>`, transcript-free property demonstrated. Architect rulings recorded: contract_version=null, exit_semantics=0, per-item extraction/record annotations, mirror block via the amended save_to_file contract. **Filed #33** (terra pass-3 waiver residual — codex credits exhausted, reset 2026-07-23), **#34** (research-path parity, R6), **#35** (broad R4 fail-loud). Terra pass-1/pass-2 clean-on-substance after fixes; pass-3 EXPLICITLY WAIVED per architect ruling.) · 2026-07-17 (**RIDER 2** — session ruling, filing only: coverage of the operator's delegation-window ADVISOR layer (caller-side authoring → sub-question decomposition → outputs read-back) checked against ADR-11 + [E1] = PARTIAL; [S10] delivered the council-side surface but the caller-side front half was uncovered. Filed **new story [S13]** with **#36** (authoring advisor; reconcile w/ #9 quality gate), **#37** (decomposition advisory; caller-side, Q5-compatible), **#38** (verdict→ADR read-back guide, F7). Zero build, zero new folders.) · 2026-07-18 (**morning close** of the 2026-07-17 night batch: **#31 struck** — [S12] delivered 2026-07-17, collapsed to a theme-level note; **#24 CLOSED by operator ruling** — the G3/Epic-B event is the recorded synthesizer ruling (`docs/audits/2026-07-17-synthesizer-ruling-gemini-to-openai.md`), the EPI-1 40-item pack retained as the reversible instrument; **Epic B un-gated** — #2 Branch A (openai) shipped as the durable config default (ADR-01 amendment text pending), #18/#19 un-gated with planning deferred to a future session; filed **#39–#43** from the night-batch E2E audit — `--no-persist` output guard, `options_considered` extractor, CLI-seat token regexes, research double-prefix, first-class codex seat name.) · 2026-07-18 (**#40 + #41 struck** — Stream B `worktree-p6-hardening` integrated to main via `--no-ff` merge `edce0e7`: #40 verdict-package `options_considered` extractor (`496a2c9`) + #41 CLI-seat token counts (`8a5cecd`); both E2E-witnessed + contract-green, primary `check.ps1` 531 passed; done-task leaves per ADR-65, git carries the record. terra WAIVED to 2026-07-23 — #40/#41 go first in the batch re-review.) · 2026-07-18 (**#39 struck** — serial build in primary after Stream B integration: `council --no-persist` (scratch temp) + `AICOUNCIL_OUTPUT_DIR` env override + bounded `output/health/` retention (keep-10 prune in `doctor.write_record`; documented in the doctor module docstring, ops home outside the CONTRACT — witness/dev controls are not the Lane-A delegation surface, so Contract-Version stays 1.0 and #34 keeps the 1.1 earmark). check.ps1 537 passed; terra WAIVED to 2026-07-23. Done-task leaves per ADR-65.) · 2026-07-18 (**integration close-out**: struck **#22 / #23 / #42** (Stream A, shipped this session — merges `3e64e81` / `04cf534` / `5b139ad`; CONTRACT §7 deviations closed + double-prefix fixed; done-task leaves per ADR-65, terra residual carried by #44). **ARC 4** stamped **Contract-Version 1.0** (`5dd4782`, §7 empty, verdict package emits `"1.0"` witnessed live). Filed **#44** (batch terra re-review, date-gated 2026-07-23; #40/#41 first, then #22/#23/#42/#39/1.0-stamp; does not subsume #33). Hand-forward clauses added to **#43** (design fork: codex has no API twin → ADR-12 same-seat-fallback assumption) + **#27** (de-risk: zero-fallback CLI E2E witnessed). **#20** stays open (openai 2.x ignores are a stopgap, not the fix).) · 2026-07-18 (**audit-remainder filing pass** — converted the deployment-status audit's untracked remainder into tracked items; no new intakes/decisions. Filed **new story [S14]** (E5, code-quality residue) with **#45** (runner→orchestrator re-export shim, SEED-5/A5), **#46** (utcnow×5 centralize, B2), **#47** (dead `_target_projects`, B4), **#48** (RunPolicy-from-YAML, B7); **#49** under [S9] (EPI-2 identity-integrity six-field check); **new story [S15]** (E7, governance mechanization) with **#50** (DRAFT-GOV-2 `## Watches` W1–W3) + **#51** (ADR-14 header↔index sync); **new story [S16]** (E3, small nits) with **#52** (DOC-2 pin-aging), **#53** (R4 misclassified-mode guidance), **#54** (research `"incomplete"`-terminal nuance); **#55** under [S8] (council-arch **G5** baseline experiment → filed as input to the T1 baseline-planning session, baseline-gated). **council-arch G1–G4 CLOSED as superseded-brainstorm** (never BACKLOG items; dispositioned won't-file): G1 full prior-round-refeed policy — kept as a live *trade-off*, not a defect, no per-regime split filed; G2 score-based aggregation — Free-MAD token economy, not adopted (qualitative judge pass retained); G3 debate-gating ("earn the debate", iMAD) — always-debate retained by design; G4 calibration/cross-session memory — conflicts with ADR-03 blind voting, not pursued. ADR-01 text drift fixed: in-body `Revised 2026-07-18` amendment marker (Gemini→OpenAI, #2/#3) + Status line + README index synced. Next free local id after #44 → #45–#55.) · 2026-07-18 (**worktree consolidation** — closed out two parallel worktrees into main. **[S14] delivered + collapsed**: struck **#45/#46/#47/#48** (code-quality residue shipped on `chore/s14-cleanup`, merge `02b6dd0`; SHAs `f7f8227`/`da7825c`/`6d55070`/`6c73686`; `check.ps1` green 543; done-tasks leave per ADR-65), added #45–#48 to **#44**'s 2026-07-23 terra re-review list (waived), refreshed the Deployment-Status stamps on `code-quality-audit.md` + `code-refactoring-guide.md` for the shipped rows. Merged the **smoke-pair** worktree (`e1af32c`) — re-homed the CLI-vs-API side-by-side report + burn note from `docs/smoke/` → `docs/audits/` (JOURNAL merge-conflict resolved keeping both entries; sealed-key leak caught + untracked before merge). **Filed #58** under [S9] (minority report emitted despite panel consensus — extractor over-fires, witnessed in BOTH arms of the 2026-07-18 smoke pair). Next free local id after #57 → #58.) · 2026-07-19 (**review debt closed** — terra re-probed live (`TERRA-OK`, $0), falsifying the "credits exhausted until 2026-07-23" premise a second time. **#33 struck** — verdict-package pass-3 ran, **PASS-3 CLEAN (no Critical/High)**. **#44 struck** — the final three surfaces (#22 `3e64e81`, #42 `5b139ad`, #39 `e140c5d`) reviewed, completing the 2026-07-18 arc set at 8/8 terra-reviewed; closed via the done-when's **"(or fixes filed)" clause, NOT because the surfaces were clean**. Findings verified against source, then filed: **#69** (P2, frontmatter `models:` dead whenever `--lite` is absent + `--file`/`--inbox` guard divergence), **#71** (P2, `--no-persist` `mkdtemp` never cleaned — a live §5.9 "No leftovers" violation, strengthens #68), **#70** (Low, research-slug same-second overwrite), **#72** (Low, over-broad `doctor-*.json` prune glob); two terra severities downgraded with reasons recorded. **#20 scope widened** to type-stub hygiene after a second missing-stub gate failure the same day (`types-PyYAML` declared in dev extras; no new ignore; `check.ps1` exit 0). Next free local id after #68 → #69–#72.) · 2026-07-19 (**Lane A2, CLI output contract** — filed **#74** (P3/S, `--output` did not `.expanduser()` while the `AICOUNCIL_OUTPUT_DIR` branch always did, so `--output ~/foo` created a literal `./~/foo` directory; surfaced while extracting the shared output-dir resolver) and **closed it in the same arc** — done-task leaves per ADR-65, git carries the record. Filed-and-closed rather than deferred because `cli.py` is the most contended file in the repo with Lane B queued behind this arc, so deferring a one-line fix would cost a whole future serialized pass over it. Next free local id after #74 → #75.) · 2026-07-19 (**three-lane reintegration** — serial `--no-ff` merges A2 → A1 → C with the operator as the gate. **Struck #35 / #62 / #63 / #60 / #65 / #71** (all discharged by merged work, each with checker-leg evidence; done-tasks leave per ADR-65, git carries the record). **#74 needed no strike** — Lane A2 filed and closed it inside its own arc, so it never had a task line. **#66 stays OPEN**, not discharged, now structurally gated on #27 (the ADR-12 §5 backend=cli flip) exactly as #41 was — its discharge leg EXISTS and correctly reports GAP rather than PASS while the run cannot be made free; no billed witness authorized. **#67/#68 deliberately NOT hand-struck** — routed to `/review-closures` under ADR-70 Tier-1, which proposes rather than auto-closes. Integration surfaced a **cross-lane seam defect neither lane could see** (both green, merged main RED): A1 changed `OutputRoutingError`'s constructor to a failure list without a type guard, and four call sites still passed a string, which `list()` shredded into one 'deliverable' per character — repaired fix-forward with a type guard + strengthened assertions, and a third composed checker `scripts/verify_output_contract_e2e.py` added so the blind spot is mechanized, not just fixed. Filed **#75** (`secondary_dir` raises where `target_paths` swallows) and **#76** (verdict `artifacts[]` built pre-write — Contract-Version 1.1 candidate alongside #34), both Lane A1 escalations deliberately outside its frozen contract. Next free local id after #76 → #77. · 2026-07-19 (**sol adversarial pass on Lane A1's merged diff** — 8 High returned; **classified REGRESSION vs PRE-EXISTING by differential run against `27a45d1`**, not by reading the diff. **2 regressions REPAIRED on main immediately** (F8 bare `"considered"` marker promoting a `## Risks Considered` section over the question's real options — `[]` became plausibly-wrong `['Risk one']`; F2 direct-mode raise costing the canonical metrics sidecar, violating the lane's own canonical-writes-land-first invariant), both proven by reversion. **6 PRE-EXISTING filed, not patched:** #75/#76 already filed from the lane's own escalations, plus **#77** (F6+F7 as ONE contract-scoped ticket — `options_considered` is corrupted on main TODAY and this is the second half-fix window on the same function), **#78** (F3 `target_paths` shape), **#79** (F4 metrics-manifest existence check). Next free local id after #79 → #80.) · 2026-07-20 (**#82 filed** under [S16], pre-implementation gate of the [#18] crux-check arc — the bounded crux-check plan's accepted Phase-A hole **E2** (research-lane retrieval cost invisible to `DebateMetrics`, so `print_cost_summary` understates spend once retrieval fires on every debate) filed as a tracked item rather than left as a code comment, per operator gate 2. Filed beside #61 as the same metrics-fidelity class. **Pointer correction:** #80/#81 were filed as design forks at `d3fd4cb` without a grooming entry, so the standing "next free after #79 → #80" pointer was stale on arrival; actual next free local id after #82 → **#83**.) · 2026-07-20 (**#83 filed** under [S16] — terra pass-3 residual on the [#18] crux-check arc: passes 1 and 2 ran and their findings were repaired, but pass 3 could not run (codex usage limit, verified live, resets 2026-07-25 18:55), so the pass-2 repairs carry no adversarial re-review. Date-gated 2026-07-25; #33/#44 precedent. Next free local id after #83 → **#84**.) · 2026-07-21 (**#2 + #3 struck** — closure hygiene from the night backlog trust audit (`docs/audits/2026-07-20-night-backlog-audit-opus.md`), the only two DONE-BUT-LISTED items in 50 open. Both done-whens re-verified first-hand against source before striking, not accepted from the audit: #2 = `config/settings.yaml:12` `synthesizer: "openai"` (`ca7e85c`, merged **`6e83e41`**) **and** the dated ADR-01 `Revised 2026-07-18` amendment (`e3bdcc8`, merged **`a854bd3`**); #3 = the cost-optimization principle present verbatim and id-tagged in that same amendment. **[S1] fully delivered → collapsed** to a one-line italic note per ADR-65 (the [S12]/[S14] precedent), since both its tasks left; [E2] retains [S2]/#4. ADR-01's Deployment-Status stamp refreshed — it still claimed `residual: #2/#3` while the amendment sat thirteen lines below in the same file, having been written by the pass that discharged it. **Drift mechanism recorded:** the close was split across two merges and each disclaimed the other's half, so no pass looked back; this was the *only* such seam in 50 items (audit A2: id-space `#1..#83` complete, zero gaps). **Knock-on left open, deliberately:** #4's condition has now FIRED (it was conditional on #2 Branch A, and its "closed as not-needed if Gemini retained" escape hatch is void) — re-scoping #4 is moratorium-held, not done here. Task count 50 → 48; stories 14 → 13. Next free local id unchanged → **#84**.) · 2026-07-22 (**#37 struck** — Unit 2 P1 `council boost` (merge `3bf3ca9`) delivered decomposition beyond the done-when: a hybrid compound is not merely flagged-with-recommendation but actually decomposed into ≥2 linked conformant sub-briefs — verified first-hand against `boost.py::_decompose` + T3/T6 (`tests/test_boost.py`, sub-briefs accepted by `inbox.parse_file`) and the 2026-07-22 live witness (deepseek decompose passed the verbatim span gate, exit 0), not accepted from the session claim. The owner-A caller-side-advisory framing is obsolete under the ADR-11 amendment owner-C ruling. **Residual recorded, not silently lost:** P1 splits research+decision compounds only — a bundle of multiple *independent decisions* classifies `decision` and is gap-flagged, not split; file a successor if that case proves real. **#36 deliberately NOT struck** — operator rules: boost functionally supersedes template-filling (a raw question in yields a conformant brief), but #36's done-when names a fill-in template artifact that does not exist. Filed **#84** (GUIDE synthesizer-default staleness, gemini→openai 2026-07-18) + **#85** (GUIDE `rounds:`-pin vs `ideas` `max_rounds: 1` divergence, terra 2026-07-22 HIGH-2 deferred to template level). Next free local id after #85 → **#86**.) · 2026-07-22 (**id-collision repair + [S13] close-out** (operator rulings). **#84/#85 → renumbered #86/#87**: the ids were reserved (unrecorded) as renumber destinations for the carried hub-id tasks #110→#84 / #128→#85 (ADR-41 re-file); verification first-hand — #110/#128 confirmed live at their hub ids with re-file provenance, but NO reservation record existed in this file or the hub BACKLOG, hence the silent collision; a standing **Id reservations** note now precedes this log so it cannot recur. **Task-count arithmetic accounted:** the operator's "47 → 49" premise was off by one — the recorded baseline was **48** (2026-07-21 entry "50 → 48", confirmed by validate_backlog this session) → −1 (#37 struck) +2 (#84/#85 filed) = **49**; no unexplained delta. **#36 STRUCK by operator ruling — superseded-by-boost:** its premise (a fill-in template the agent authors from) is void under owner-C — the caller submits a raw question, it does not author; `council boost` (merge `3bf3ca9`) yields the conformant brief. One genuine remainder filed as **#88** under [S13] (caller-facing boost invocation note as a SECTION of `protocols/COUNCIL_INVOCATION_CONTRACT.md`, not a new document; distinct from #38's read-back side). **[S13] retitled** per ruling: "Stand up the commissioning window — council-side input stage + caller-facing docs". Count: 49 −1 (#36) +1 (#88) = **49**. Next free local id after #88 → **#89** (with #84/#85 reserved).) · 2026-07-23 (**session-close absorb** — the three-window debt closed: the 8 live-unfiled P1s from `docs/audits/2026-07-20-night-code-audit-opus.md` filed after first-hand re-verification of each against current source (none had been fixed incidentally since the audit): **#89** P1-6 degraded-cache, **#90** P1-10 summarizer no-timeout, **#91** P1-11 timeout substring-sniff (all under [S3]); **new story [S17]** (E5, successor class to delivered [S14]) with **#92** P1-12 `cli.run()` complexity (filed with ARCHITECTURE invariant 2 attached as the target per operator ruling — invariant NOT reworded to match code), **#93** P1-13 `output.py` four-modules, **#94** P1-14 tautological test, **#95** P1-15 blind-voting test asserts nothing, **#96** P1-16 orchestration mocked out of existence. True P1 accounting recorded: 16 total = 6 deliberately fixed (in-code `P1-n:` comments) + 2 pre-filed (#69/#75) + these 8. **New story [S18]** (E7) with **#97** — the claim-vs-reality checker v1, twelve rules from the night-batch spec (`2026-07-22-pre-handoff-cleanup.md` §3), each traced to the drift it would have caught, rule 5 refined by this session's Block-2 archaeology (the panel-default "conflict" dissolved: `cli.py:689/812` `use_full_panel or not lite` makes full_panel the effective default exactly as ADR-02 documents — config-claim parity must adjudicate effective behaviour, not raw keys). Count: 49 +9 = **58**; stories 13 +2 = **15**. Next free local id after #97 → **#98** (with #84/#85 still reserved).) · 2026-07-23 (**record fixes** (operator-approved set). **MORATORIUM LIFT RECORDED:** the 2026-07-21 intake/ADR moratorium that held #4's re-scoping ("moratorium-held, not done here") was **lifted mid-window by the operator but never recorded in the repo** — recorded here now; this is why two passes conservatively left #4 untouched. **#4 re-scoped in place** accordingly: (a) ADR-02 overlap-policy amendment + (b) stale stamp refresh, with (c) the panel-default question explicitly DISSOLVED (2026-07-23 archaeology; batch-report amendment) and fenced out of the amendment's scope. **#96 → renumbered #98** (P1-16): #110/#128 carry `refs #96` in the HUB id space (closed upstream; 2026-07-20 audit A1 "dangling") — assigning local #96 made those refs silently resolve to an unrelated ticket, worse than dangling; verified first-hand by repo-wide grep before repair. **Id-reservations note EXTENDED** to cover dangling-reference targets (not just planned renumbers): #96 now reserved until the #110/#128 renumber resolves or drops its refs; rule = grep before assigning any new id. **#69 gains a verified P2 constraint:** its gating expression `(use_full_panel or not lite)` (`cli.py:689`/`:812`) is the SAME expression that makes full_panel the effective 5-model default — a parity fix can silently flip the panel default 5→3; decouple + pin with a regression test first. **#99 filed** under [S15]: pointer to the un-triaged night-batch proposal set (`2026-07-22-pre-handoff-cleanup.md` §2) so the BACKLOG-only reader sees it exists (fresh-architect gap 2); individual triage deliberately deferred. `scripts/council-ask.ps1` **deleted** (operator-approved; referenced by nothing — batch §2.5 grep evidence). Count: 58 +1 (#99) = **59** (renumber is net 0); stories **15**. Next free local id after #99 → **#100** (reserved: #84, #85, #96).) · 2026-07-23 (**#97 extended 12 → 14 rules** (operator defect-close before the `docs/architecture-repair` merge): **rule 13** ticket-reference resolution — every `#<id>` in canonical docs resolves to a live item, a reserved id, or a historical marker (earned by the #84/#85 collision, the dangling hub `refs #96`, the stale "pending #2" clause, and the "#97 rule 14" forward reference itself, which the rule caught in its own author's edit); **rule 14** layer-edge conformance — every codemap edge inside the declared allowed set or a named open case (earned by `cli -> boost` unnoticed for a day; ARCHITECTURE's allowed set completed 2026-07-23 to give it a gate). **Rule 5 refined**: read the audited document's own explanatory sections before reporting a contradiction — the panel-default false positive existed because Key Design Decisions explained the indirection in the audited file itself. ARCHITECTURE's "rule 14" citation now resolves. Same pass verified the E4 exit-3 claim against source (`boost.py:550-552` degraded=True + full-text legs; `cli.py` `sys.exit(3 if result.degraded else 0)`; pinned by `test_boost.py:275/329-337/359-372`) and the layer-edge list render (8 separate items on disk; the joining was a diff artifact). No id consumed; count **59**, stories **15**, next free **#100** (reserved #84/#85/#96).) · 2026-07-23 (**#97 rule 14 given two legs** (operator: vacuous as first written — leg (a) codemap-vs-allowed-set validates the map against itself; an illegal import the map omits passes). Leg (b) added: every real `src/` inter-module import appears in the codemap. **Leg (b) validated live the same hour:** the operator's two-edge hypothesis (`cli -> inbox`, `cli -> config`) grep-confirmed — and the full `cli.py` import list shows **~11 real module edges absent from the codemap** (inbox, healthcheck, mode_detector, models, output, policy, providers, routing, runner, config, function-level research), so ARCHITECTURE's "cli -> boost is the only layer-skipping edge" claim and its allowed edge set are map-derived understatements; follow-up ARCHITECTURE edit deferred to an operator ruling (report-only per instruction). No id consumed; count **59**, stories **15**, next free **#100**.) · 2026-07-25 (**filing batch — one operator window converted to tracked work; BACKLOG-only surface, nothing else edited.** Filed **12**: **#100** verdict-package JSON Schema (1.1 cut alongside #34/#76, [S10]); **#101** boost sharpening-annotation block + **#113** the interactive clarify-loop ([S13]) — ruled **two tickets, not a rider**: "rider" named the relationship, `refs #101` carries it, and a clause inside #101 would either block that line's closure or vanish with it, carrying no open-id status and unable to enter the queue; #113's done-when names **both** exits (built per Design B, **or** the ADR-11 decision-1 ruling forecloses interactivity and it closes as deliberately-not-doing) so a ruling that goes the other way cannot leave a zombie; **#102** next-up ranked queue, two legs ([S15]) — leg (ii) the session-close regroom advisory is what stops "re-groom at every close" being prose, and the **numbered-list queue shape** (`1. #100 — label`, never `- [#id]`) sidesteps both `validate_backlog` parse failures with **no grammar change and no ADR-78 hub coordination**; **#103** #19 framing-defense DESIGN commission, explicitly not a build ([S9]); **#104/#105/#106/#108** the #97 Unit-1 checker follow-ups ([S18]) — #104 (promotion to gating, one line at the `check.ps1` call site) is **blocked by #105 AND #108**: a "0 findings" gate is unpassable while 4 dangling SHAs yield 6 findings and self-replicate, and R2 cannot earn its clean window while 3 of its 4 first-run findings are context-blindness false positives; **#109** the undocumented `verify_*`/`validate_*` prefix convention ([S7]); **#111** CLAUDE.md's `logs/TOKEN-LOG.md` citation ([S15]) — triaged as **stale addressing, not a missing file**: `git log --all` on the path is empty, the log is the hub's (`.dev-knowledge/logs/TOKEN-LOG.md`) exactly as `INSTALL.md:33` says, and that hub file is what `/session-summary`'s preflight read at JOURNAL:1885; **#112** the four dangling JOURNAL SHAs ([S15]). **Mechanism, not prose:** `gc.auto` set to **0** repo-locally (`.git/config`; was unset = git default 6700) **before** any branch — #112's "do not run gc" cannot stop `gc --auto` firing after commit/merge/receive-pack, and the commits date from 2026-07-09, past the two-week prune horizon, so age no longer protects them; restoring `gc.auto` is part of #112's done-when. **5 rider edits, no ids consumed:** #80 done-when → the CommonMark continuation rule (spike-proven); #81 done-when → KEEP-SCANNER + hand-ported fence-skip (evidence at tag `spike/md-parser-evidence`, verified present); #92 done-when → re-derive the ARCHITECTURE allowed-edge set, the FLOOR1 R1 trigger that sat at `ARCHITECTURE.md:180-181` as doc prose with **no task carrying it**; #19 → design-predecessor pointer to #103; #50 → W4 watch candidate (JOURNAL drifting from decision-record toward stop-log — ADR-85's gate fires per session-STOP, not per substantive change). **One carried item re-triaged, not filed:** `protocols/AI_COUNCIL_PROCESS.md` arrived as genuine drift and is a **checker false positive** — the file exists at the hub and `protocols/README.md:20` says so in prose, while `_R2_ALLOWLIST` keys on the prefix *inside* the backticks; folded into #108 as leg (b). **#107 newly reserved** (third hub/local id collision; rule 13 named as the fix, with a concrete test case recorded). Gotchas (`git merge -F -` stdin dash, `check.ps1` exit-leak, the ADR-85 per-STOP anchor) route to `~/.claude/skills/gotchas/gotchas.md` in a separate window, deliberately not into BACKLOG. Count **59 → 71**; stories **15** (no new story). Next free local id **#114** (reserved: #84, #85, #96, #107).) · 2026-07-26 (**#112 CLOSED** — the four JOURNAL-cited dangling SHAs are now reachable from **pushed annotated tags**, not from an uncommitted local config value. The 2026-07-25 tag `archive/journal-26192dd` (annotated, never pushed) was **deleted and replaced**: the namespace is now `archive/cited-<sha>`, renamed on the operator's ruling so the tag states its retention predicate rather than implying the commits are journal entries — three of the four are 2026-07-08 QA probes, not journal work. Created and pushed to `origin`: **`archive/cited-26192dd`** (spike-arc journal anchor, branch never merged), **`archive/cited-6a44191`** (qa-n4 direct-commit-on-main probe — the F1 finding; hard-reset away, never pushed), **`archive/cited-0b77a67`** (qa lived-exercise `clamp` toy feature), **`archive/cited-bd3a17c`** (qa-n3a `last_reviewed` probe). Each annotation carries its **own** reason — the exact citation `file:line` list, "reachable only via this tag; unreachable in the commit graph otherwise", what the commit is, and `refs #112` — so a reader who finds the tag needs neither this log nor the ticket. **Why tags beat the config value:** `gc.auto=0` lived only in `.git/config`, untracked, so no clone and no worktree inherited it (see the gc-auto memory); a pushed tag is a real ref that survives a clone, a fresh worktree, and a local config reset. **`gc.auto` restored** to the git default (`git config --unset gc.auto`; local key now absent, effective 6700) — the disabled-maintenance debt is discharged and `git gc` / `git prune` / `git reflog expire` are safe again. **Verified:** `git describe --all --contains` resolves all four (was `fatal: cannot describe` on three, and tag-only on the fourth); `git ls-remote --tags origin` shows all four on the remote; `validate_claims.py` rule 8 moved **5 findings → PASS / 0**, SUMMARY `pass 3 | FINDINGS 4 across 1 rules | anchor-missing 0 | skipped(Unit2) 7 | errors 0` — the 4 remaining are all pre-existing R2 path drift (#111 token-log, #108 leg (b) `protocols/AI_COUNCIL_PROCESS.md`, and the two `.claude/skills/` negation-class entries), so **#104's gating precondition is now half-met**: #105 remains. **JOURNAL untouched** — it is append-only and its citations resolve as written now that the commits are reachable, so no correction entry was owed; the ticket's own cited line numbers (`:26`/`:788`/`:1947`/`:1949`) had already drifted to `:60`/`:822`/`:1981`/`:1983` (34 lines prepended since filing), which is why the tag annotations date-stamp their line list as a snapshot and name the drift direction. Count **71 → 70**; stories **15**. Next free local id **#114** (reserved: #84, #85, #96, #107).) · 2026-07-26 (**#27 grooming note — finding only, nothing edited:** the CLI-4 parity instrument was re-checked against live state and **API is still the default**, confirmed first-hand on both surfaces — `ADR-12:33` "(default `api`)", `:63` "`standard` (default *after* the flip clause)" and `:76-79` "then `backend: api` remains the default everywhere" (the clause is still evidence-gated, unflipped), plus `READING-INDEX.md:29` stating the flip as prospective ("PASS across all items → ratify DRAFT-CLI-3 (amend ADR-12 §5, flip `standard` default to CLI)"). The instrument therefore **stands as written** and **#27 remains the authorizing gate** for the unseal — so **#27, `READING-INDEX.md`, `SCORING-SHEET.md` and ADR-12 were all deliberately left untouched** (recorded here rather than on the task line, since amending #27 to say #27 is unchanged would be the edit this note exists to avoid). No id consumed; count **70**, stories **15**.) · 2026-07-26 (**#97 F1 — registry repair; gates (i) and (ii) MET, ticket stays OPEN.** `RULES` held **eleven** legs against a **fourteen**-rule spec: rules **1** (module-table completeness) and **7** (stamp honesty) were absent entirely — no leg, no stub row, no `rule_id` — and nothing could see it, because the report's KNOWN LIMITATIONS named only the seven stubs and the registry test iterated `vc.RULES` itself, so it covered whatever was present and **structurally could not detect an omission** (41 tests green throughout). Repaired in three parts. **(a) Test first, RED first** — the external expected-set test landed *before* the registration, so the proof is in the history rather than asserted after the fact: `RULES drifted from the #97 spec: missing [1, 7], unexpected []`. Its `{1..14}` literal is deliberately **not** derived from `vc.*`; deriving it would restore the very blind spot. **(b) Rules 1 and 7 registered** as `_unit2_stub` rows — no new mechanism, registry now 13 legs, rule 12 still the one sanctioned exemption (structural, in the `Finding` model). **(c) Coverage block, computed not hardcoded** — KNOWN LIMITATIONS now prints implemented (4) / stubbed (9) / structural (1) / absent (0), TOTAL 14 of 14; the counts come from the run, so `absent` is a **live** number. A hardcoded roster would go stale the first time a stub is implemented, which is the exact drift class the checker exists to catch. **Independent derivation:** sol (`gpt-5.6-sol`) re-derived the expected id set from this file's #97 line alone, fenced off from `scripts/validate_claims.py` and the test file, and returned `{1..14}`, count 14, contiguous, rule 12 flagged as the non-leg exemption — two independent derivations agreeing is what makes the denominator evidence rather than an echo. **Seeded-omission proof (both legs):** dropping the rule-7 row yields `missing [7], unexpected []` from the test and `absent      (1): 7` from the live report; restored, 48 passed. **Residual disclosed, not buried:** `_SPEC_RULE_IDS` is hand-copied from the #97 line, so a fifteenth rule in the spec would leave both literals at fourteen and everything green — the report **says so** and points at the new **#114**, filed under [S18] with a fail-loud constraint on the parser (a parse yielding ≠14 ids must be a CHECKER ERROR, never a silent pass) because a brittle regex would trade a disclosed gap for an undisclosed false pass. **#97 NOT struck** — its seeded-violation-of-each-rule clause needs the nine stubs implemented. Count **70 → 71**; stories **15**. Next free local id **#115** (reserved: #84, #85, #96, #107).) · 2026-07-26 (**F2 — the ruling batch: nine rulings converted from chat into repo records.** Filed as BACKLOG edits only; no code, no ADR, no intake. The recurring failure this closes is decisions surviving only in a transcript, so each ruling is written where the next session reads, not where this one happened. **R1 → new #115 ([S17]):** self-referential verification gets **two homes** — a `LESSONS.md` entry for the class and a **collection-time assertion** that any `parametrize` over a module-level registry has a companion external expected-set test — and is **explicitly NOT a checker rule for v1** (it is a property of the test suite, not doc-vs-reality drift, and `validate_claims.py` would have to introspect the very registry it checks). **R2 + R5 → #104 rewritten:** #105 is **struck from the blocker list** — its self-replication premise died when R8 reached zero, so the count no longer outruns repair — and **#116 added**, which #104 had never named. The prose trigger "one clean window per rule" is **replaced**: it named no window length, no defect unit and no checkout, so no run could satisfy or fail it. Promotion now requires all three of (i) an external denominator test, (ii) identical results on a fresh clone at the same commit, (iii) zero **distinct defects** on every first-parent commit of `main` across >=5 consecutive commits. #105 stays open because (iii) is stated in distinct defects — it **measures** the criterion instead of blocking it. **R3 → recorded on the new #116:** terra's H3 disposition set its revisit at "gating promotion"; the trigger **fired early** for two independent reasons — R8 reaching zero made R2 the entire finding set rather than one contributor, and the checkout-nondeterminism arrived by a route H3 never considered (checkout-dependence, not precision), so it is a new defect in the same organ rather than the tradeoff being re-litigated. The guard ruling itself **deliberately waits on the STEP-3 measurement** — ruled on what the guard suppresses, not on what survives it. **R6 — recorded as DONE, note only:** ADR-12 section 5 is **unamended**, **DRAFT-CLI-3 has no file vehicle** (it exists as a draft name inside ADR-12 and the parity instrument, not as a document), and ratification is therefore an **ADR-12 amendment gated on #27**, not a separate artifact anyone needs to go find. No ticket: nothing is outstanding beyond #27 itself. **R7 → new #117 ([S13]):** the ADR-11 decision-1 interactivity ruling now has an **owner**. #113 was correctly blocked but its blocker was unowned, so it could sit forever; #117's done-when requires the ruling **and** that #113 be driven to one of its two already-named exits, because a recorded ruling that leaves #113 open reproduces the zombie it exists to prevent. **R8 — scope NARROWED by the tags:** the bare-SHA-citation convention (rewrite citations into a prune-surviving form) now matters **only for genuinely unreachable SHAs**. The four that motivated it are reachable from pushed `archive/cited-*` tags, so their citations resolve as written and need no rewriting; the convention survives as a rule for future dangling SHAs, not as outstanding work. Note the order this establishes: **make the commit reachable first, rewrite the citation only if you cannot** — cheaper, and it preserves the history the citation points at. **R9 → #73 / #8 / #87 done-whens ruled, plus the review-lane routing posture given a repo home.** #73: point at the **global** `/codex-review` → `~/.claude/bin/codex-review.ps1`, do **not** re-home a repo-local wrapper; re-verified end-to-end this session (it produced `docs/audits/2026-07-26-codex-97-registry-repair.md`), a third witness after N5a and the 2026-07-20 half-verification, so the verification half is discharged and the done-when narrows to applying it in CLAUDE section 8. The **routing posture** — terra/sol/luna = `gpt-5.6-terra`/`-sol`/`-luna`, never a bare `gpt-5.6`; review script for diffs, direct `codex exec -m gpt-5.6-<lane> -s read-only` for bounded non-diff prompts — existed only in `~/.claude` and JOURNAL and is now a repo record. #8: **amend ADR-34 with an ISO-timestamp exemption**, do not change the emitter (the underscore separates date from time *inside* one token; hyphenating it makes the timestamp/slug boundary unparseable and orphans every existing `council-out-*` filename). #87: **drop `rounds:` from the canonical template** — a doc pinning a number `settings.yaml` owns is CLAUDE section 5 item 8 inverted; `rounds:` stays available as a deliberate per-brief override. **#8 and #87 are executor rulings under this batch's mandate** and are flagged as such in-ticket: both are reversible in one line while they are still BACKLOG records, and neither has been applied to code or ADR. **Four cheap forks ruled, all previously unowned:** **#43** no-fallback **sentinel**, not a mapping — mapping `codex` onto another vendor's API class re-creates the identity-riding defect one level down, and under ADR-03 blind voting a silent substitution is worse than a hard failure because the panel cannot see it; consequence recorded, it needs an **ADR-12 amendment** sequenced with #27's unseal. **#6** **keep**, and convert the line from an open evaluation to **watch W5** under #50 — a reactive trigger that has never fired is a permanent zombie as a task; **no failure-rate measurement was taken and none is claimed**, the ruling is about the item's shape. **#56** **banner**, not rename — a rename invalidates every reference, needs an ADR-60 amendment, and would trip #68's guard (which keys on sanctioned folder NAMES); the problem is legibility, so fix legibility. **#109** **write it down** — the `validate_*` = gate / `verify_*` = witness split is already real and consistent, so dropping it discards a working convention; ARCHITECTURE section Validators states it and names the three exceptions as exceptions. **Id check recorded (rule 13 discipline):** #115/#116/#117 grepped before assignment. #115's apparent hits are all noise (`.venv` line numbers, a council transcript, this log's own next-free pointer). The hub `.dev-knowledge/BACKLOG.md` **does** hold its own #116 and #117 — hooks-hygiene and prompt/agent-hook evaluation — but they are global-infra items that will not be re-filed here under ADR-41, and nothing in this repo refs them, so there is no dangling-reference hazard; recorded here rather than reserved, since the #110/#128 collisions happened precisely because an id decision went unwritten. Count **71 → 74**; stories **15** (no new story). Next free local id **#118** (reserved: #84, #85, #96, #107).) · 2026-07-26 (**#111 CLOSED — and it was not the stale-reference correction it looked like.** Both citations sat inside **owner=hub** methodology regions (`critical-rules-records` sections 5 item 1, `antipatterns-universal` section 10) carried **byte-verbatim** from `templates/claude-regions/`, so correcting them locally is a **fleet-methodology divergence**, not a typo fix — surfaced before editing rather than discovered after. Both now read `.dev-knowledge/logs/TOKEN-LOG.md`; `last_reviewed` re-stamped; CLAUDE section 12 gains v2.15. **The root cause is position-dependence, not wording:** the bare path is correct AT THE HUB and false at every consumer, so **no byte-verbatim template can be right in both places** — it needs a substitution mechanism, and a naive hub-side text fix would merely invert the error. Filed upstream **the same pass** as hub **#427** (hub `main` `863cb804`, pushed), and the local `claude-md-token-log-address` divergence in `.methodology.yaml` **names #427 as its explicit retirement trigger**, so it expires BY REFERENCE instead of becoming a permanent record of a temporary condition. **The R2 allowlist was refused** — it would hide the class rather than adjudicate it, which is the same reason #108 was built as a predicate. **Report reaches FINDINGS 0** (`pass 4 | FINDINGS 0 across 0 rules | anchor-missing 0 | skipped(Unit2) 9 | errors 0`); rule 2 PASSES for the first time. Hub-filing note worth keeping: the hub's primary checkout was on another branch with concurrent uncommitted work, so #427 was filed from an isolated worktree off hub `main` — and that worktree had to be provisioned per the hub's own `.worktreeinclude` (`ecosystem/*/state.yaml` is untracked; without it the `audit-health` gate reports `repos registered (none)` and blocks the commit). Count **74 → 73**; stories **15**. Next free local id **#118** (reserved: #84, #85, #96, #107).) · 2026-07-26 (**Checker to zero, guard rebuilt, review debt cleared.** **#108 CLOSED** — R2 and R8 now share ONE pure sentence-scoped predicate: R2 consumes it as suppression, R8 as classification on the externally-attributed class alone. Three classes, deliberately narrow (negated / externally-attributed / hypothetical), because a context predicate trades a false positive for a false NEGATIVE and the negative is the worse trade — an over-broad marker silently suppresses real drift. **No allowlist entry added**, pinned by a test on `_R2_ALLOWLIST`. Findings 4 → 1. **#116 CLOSED** — the ruled resolution model replaced the first-segment heuristic: candidates resolve against the **git-TRACKED tree** under declared bases, never the disk, so R2's verdict is a function of the commit alone. Exclusions are now **declarations** rather than side effects (`_R2_EXTERNAL_PREFIXES` for ecosystem paths and org/repo slugs; `_R2_RUNTIME_PATHS` for gitignored-by-design runtime dirs), and both lists **self-validate** by test, as the base list was required to. **Two deviations from the frozen spec, both stated not folded in:** (1) a **fourth base** `docs/decisions/` was needed or `transcripts/` and `handoffs/` — 2 of the 16 — would still fire; (2) `_R2_RUNTIME_PATHS` (`output/`, `council_inbox/archive/`) was **not predictable from the drop-set**: those tokens never reached the old guard because they exist on disk and the old disk check passed them, so changing the guard surfaced a population no measurement of the old guard could have shown. Declared rather than derived from `git check-ignore`, which also reads `.git/info/exclude` and the user's global excludesfile — both untracked, and both would reintroduce the very checkout-dependence being repaired. **Acceptance met and reported as two runs:** this checkout (`output/` and `logs/` present on disk) and a fresh clone at the identical commit (both absent) each report `pass 4 | FINDINGS 0 across 0 rules | anchor-missing 0 | skipped(Unit2) 9 | errors 0`. **#106 CLOSED** — promoted from harness polish to defending rule 12's exemption, which rests on "every finding carries a re-runnable command by construction": `shlex.join` emitted the POSIX `'\"'\"'` splice that PowerShell cannot parse, in a PowerShell-first repo. Fixed as quoting STYLE, verified by **executing** the identical printed string in both shells (exit 1 each, matching `reproduces="exit-nonzero"`), not by inspection. **#104 UNBLOCKED and PROMOTABLE, deliberately NOT promoted** — criteria (i) and (ii) are met and #105 was already struck as a blocker, but (iii) needs zero distinct defects across >=5 consecutive first-parent commits of `main` and the first clean commit is the one that landed this; arming now would assert a run of evidence that does not exist. **#83 DISCHARGED via its "or its findings are filed" clause — NOT because the surface came back clean.** terra pass 3 finally ran (direct `codex exec`, since the arc is long merged and the review script's empty-diff guard would skip it) and returned **2 High**, both **reproduced first-hand before filing**: **#119** — `_REFUSAL_MARKERS` substring matching classifies *"Whether an AI cannot determine tumour grade from images is the crux"* as MALFORMED, because `"AI cannot"` contains `"i cannot"`, so a valid crux is discarded and reported as retrieval-unavailable (invisible at the call site: it looks like a provider failure); **#120** — `check()` docstrings "Never raises" at `:203` while `:212` catches only `ProviderError`. Witness: `docs/audits/2026-07-26-codex-crux-check-pass3.md`. **#118 filed, file-only as ruled** — ARCHITECTURE writes 16 paths base-relative without declaring the base, so after #116 the convention exists **only** in the checker's `_R2_BASES`, which is the wrong home for it; the ticket forbids resolving it by adding bases. **#73 gains the standing routing correction:** a producer never reviews its own output — any terra-produced diff is reviewed by **sol**; terra remains correct for CC-produced diffs, which is what ran this session. Count **73 → 72** (−4 closed/discharged, +3 filed); stories **15**. Next free local id **#121** (reserved: #84, #85, #96, #107). · **terra pre-merge review of the #116 diff returned 3 High, and two were real defects in work this session had already called done** — recorded because the near-miss is the point. **H1 (fixed):** the resolution model read `git ls-files`, which reports the **INDEX**, so a staged-but-uncommitted path satisfied a claim and the verdict still depended on working state. Index and commit tree agree on a clean tree (246 == 246 here), **which is exactly why the fresh-clone acceptance run passed while the model was still wrong** — the acceptance test could not have caught it. Now reads `git ls-tree -r HEAD`, with a test that stages a path without committing it. Evidence moved to `git cat-file -e HEAD:<p>` for the same reason, and one pre-existing test that pinned `returncode == 1` was corrected to `!= 0`, which is what `reproduces="exit-nonzero"` actually promises. **H3 (fixed):** the runtime-path self-test used `git check-ignore`, which also consults `.git/info/exclude` and the user's global excludesfile — untracked, so the test could go green on local config. It now reads the **tracked** `.gitignore`. The checker had deliberately avoided `check-ignore` for exactly this reason and the test then reintroduced it, which is worth remembering: a determinism rule obeyed in the implementation and broken in its own test buys nothing. **H2 (recorded, not patched):** any-base acceptance masks a missing path at its written location — the ruled model's inherent cost, filed onto #118 with a required collision test rather than patched inside #116. Witness: `docs/audits/2026-07-26-codex-116-resolution-model.md`.) · 2026-07-26 (**Stream A — the record remainder. Documents only; no code, no pytest.** **Renumber arc CLOSED** after being carried since 2026-07-08: **#110 → #84** and **#128 → #85**, landing both carried hub ids in the local id space. Every live `refs #96` resolved in the same edit as the audit required — both are now **hub-qualified inside #84/#85** (`hub #96`, closed upstream 2026-07-20, HUB id space), kept qualified rather than dropped so provenance survives. **#96's reservation is therefore DISCHARGED and #96 is FREE**: it existed only because #110/#128 carried a bare `#96` a local assignment would capture, and that cause is gone; the surviving mentions (#97's rule-13 justification, #98's renumber provenance, this log) are historical prose that names the HUB space explicitly. **#84/#85 reservations discharged too — those ids are now LIVE TASKS, not free ids.** Audits and JOURNAL were deliberately NOT edited: they are immutable and append-only, and their #110/#128 mentions are a correct record of the pre-renumber state. **ADR-15 authored** — the window's one real record gap: the path-resolution ruling existed only in a commit message and a ticket. It carries the three candidate models and why the first two failed; the `git ls-files` (INDEX) vs `git ls-tree -r HEAD` (COMMIT TREE) distinction **and why it was invisible to the acceptance test** — index and tree are equal on a clean tree, and a clean tree is what repo policy REQUIRES, so the acceptance compared two things policy guarantees are identical and could never have gone red; the four declared bases including why `docs/decisions/` was **required** (allowlist reachability for the hub-routed `handoffs/` and `transcripts/`, not a widening of disk resolution) **and recorded as a cost, with an explicit "do not resolve future cases by adding bases"**; `_R2_RUNTIME_PATHS` and why **no measurement of the old guard could have predicted it** (those tokens never reached the guard — the old disk check passed them, so the drop-set was a complete measurement of the wrong thing); and the self-validating rule that every declared base must exist in the tracked tree. Id derived, not guessed: local space is ADR-01..ADR-14 (the 93–101 hits are ECOSYSTEM ADRs owned by the hub), max **14**, next free **15**, zero references anywhere. **No doc-claim tracks an ADR count** — but **rule 4 tracks the ADR SET**, so CLAUDE section 11 and `docs/decisions/README.md` were updated in the same commit and rule 4 stays PASS. **#104 criterion (iii) amended to PER-RULE.** Read report-wide it was absurd: a newly implemented rule finding a REAL defect would reset the promotion clock for every already-clean rule, punishing the checker for working and creating a standing incentive not to add rules. Promotion is per-rule, and **a rule's clock restarts when its own implementation changes**. Measured by running each first-parent commit's OWN checker in a throwaway clone: **rule 3 = 11, rule 4 = 11** (both already clear >=5), **rule 8 = 4** (clock restarted at `e45c193`, one short), **rule 2 = 2 and restarting again now** under ADR-15. **LESSONS gains one entry for the CLASS, not the bug:** a value published without the predicate that produces it cannot be checked — five instances (registry parametrized over itself; ARCHITECTURE's edge count with no edge rule; R8's finding count with no defect unit; the hub's `logs/OVERRIDES.md` counting override events with no cause field, so the rate cannot separate "operator bypassed a correct gate" from "gate misattributed authorship"; and the acceptance that could not fail). **Item 5 of the prompt was DROPPED as already-filed** — the ARCHITECTURE base-relative shorthand is #118, filed 2026-07-26; re-filing it would have been the duplicate this batch exists to avoid. Count **72** unchanged (a renumber is net 0; no new task filed). Next free local id **#121**; **reserved: #107 only** — #84/#85 are now in use and #96 is released.) · 2026-07-26 (**#121 filed — the unattributed-checkout condition, with evidence rather than a theory.** The primary's reflog carries `HEAD@{6} 13:48:32 checkout: moving from fix/111-token-log-address to main`, issued by no session command; no in-repo cause exists (no hook/script/pre-commit stanza executes a checkout, every test git call is `-C`-scoped). **Precedent, not a one-off:** the hub's `logs/OVERRIDES.md` records the ADR-85 gate firing twice on 2026-07-25 against a concurrent session's commits in the shared primary tree, and names its own defect — attribution derived from commits-ahead-of-base **cannot distinguish authors**. **The class is the LESSONS 2026-07-26 class:** a value published without the predicate that produces it — `commits-ahead-of-base` proxies for "this session's work" while its real predicate is "commits on this branch not on the base, by anyone", and the two coincide only under a single-writer precondition that is asserted nowhere. Cross-referenced to **hub #423** (sequencing half; this is the attribution half) and flagged as **needing a hub twin**, since the condition is fleet-level. Consequence recorded for process: a record-HEAD → act → verify-first-parent bracket is a **detector, not a fix** — Stream A's merge used one and it held, which proves only that nothing moved that time. Count **72 → 73**; next free **#122** (reserved: #107).) · 2026-07-26 (**Four rulings recorded; promotion HELD.** **Promotion held on evidence, not caution:** rules 3 and 4 both clear criterion (i) — each has a denominator independent of the artifact it checks — and both were measured at 11 on (iii), but **neither clears (ii)**, shown empirically: a dirty uncommitted `.pre-commit-config.yaml` flips rule 3 PASS→FAIL, and a single **untracked** `docs/decisions/ADR-99-scratch.md` flips rule 4 PASS→FAIL, where a fresh clone at the same commit passes both. **#104 gains criterion (iv)** — for a GATING rule, `anchor-missing` is a FAILURE, not a WARN, because a reworded section heading otherwise lets the audited document silently un-gate the rule that checks it; the WARN semantics stay for non-gating rules, so (iv) is a promotion-time status flip rather than a harness-wide change. **Clock reset recorded:** all four implemented rules restart their (iii) windows at the harness fix, and **the measured 11 was never credit** — (iii) is conjunctive with (ii), and (ii) was never met, so it was 11 clean runs of a rule that could not satisfy the criterion it was counted against. **#121 gains the corrected BRACKET:** the one-condition form cannot distinguish *the hazard occurred* from *the operation never ran* — a failed merge leaves HEAD unchanged and the check reads the old commit's parent. Witnessed exactly so this session: `git merge -F -` errored, the merge never ran, and the bracket cried mutation. Correct form is **two conditions in order — assert HEAD CHANGED, then assert the new first-parent equals the recorded value**; conflating them trains the operator to discount the check, which is worse than not having it. **#122 filed** — the `git merge -F -` gotcha itself, named as unfiled in the last handoff supplement and now having cost time twice; it recurs because `-F -` IS correct for `git commit` and `git tag -a`, so the wrong generalisation is the natural one. **#123 filed** — the root cause the venv-per-worktree only routes around: `check.ps1` calls bare `pytest`/`mypy`/`python` and the SYSTEM interpreter carries both pytest and an editable `ai_council` pointing at the primary, so every gate run in this repo has executed on the system interpreter. Scoped as a **workflow change, not a venv build** — the primary already has a correct venv, nothing uses it — with the order marked load-bearing (repoint every caller FIRST, uninstall the system package SECOND) and marked **do not execute inside another arc**. Count **73 → 75**; next free **#124** (reserved: #107).) · 2026-07-26 (**Stream B unit (a) integrated (`cd54d9a`, bracketed, both conditions OK); #124 filed; the seven Unit-2 stubs HELD.** **#124 — REPRODUCIBILITY, and deliberately NOT folded into #123:** a clean-room `pip install -e ".[dev]"` resolves **mypy 2.3.0** and **google-genai 2.14.0**, and `mypy src/` then fails with 3 errors (`inbox.py:137`, `gemini_research.py:88`/`:96` — the genai 2.x union shape). The primary's gate is green only because its interpreter happens to hold mypy 1.19.1 / genai 1.73.1, versions **nothing in the repo pins**. #123 is *which interpreter runs the gate*; #124 is that **the declared ranges do not reproduce a green gate at all** — so #123's fix would make this failure visible rather than fix it, and #124 is therefore a **prerequisite** for #123, not a sibling. Same class as LESSONS 2026-07-26: "the gate is green" published with its real predicate — *on this machine, with these accidentally-held versions* — left unstated. Filed **P2**, with an explicit warning not to "fix" it by pinning to whatever the system holds, which would freeze the accident into the declaration. **#97: the seven remaining Unit-2 stubs (5/6/9/10/11/13/14) are HELD BY ARCHITECT RULING** — diminishing value against the mission; the checker already earns its keep on the drift classes that actually bit, and the rest are speculative against a now-clean canonical surface. **Revisit when a drift the held rule would have caught actually bites**, then build that one rule, earned by the incident. **Held, not forgotten and not silently dropped:** they stay REGISTERED, so every run still prints them as SKIP inside the computed coverage block (`stubbed (9)`, `TOTAL 14 of 14`) — which is exactly why registration was insisted on over deletion: a dropped rule vanishes from the report, a held one keeps saying "not checked". **#119/#120/#105 stay filed and unscheduled** under the same ruling. Count **75 → 76**; next free **#125** (reserved: #107).) · 2026-07-26 (**#124 BLOCKS #123 — operator ruling, recorded in BOTH tickets.** A one-sided dependency is not a dependency: #124 already said "this blocks it" while #123 said nothing, so a reader arriving at #123 would have seen no constraint. Both directions now carry the reason, not just the pointer: **#123 repoints `check.ps1` and the hooks at `.venv`, and a `.venv` built from the declared floors is a RED gate** (mypy 2.3.0 + google-genai 2.14.0 fail `mypy src/` with 3 errors) — which is exactly what #124 describes. Executed in the wrong order #123 does not surface a latent problem gracefully, it **breaks the green gate on the day it lands**, and the breakage presents as #123's own regression so the diagnosis starts in the wrong place. Land #124 first and #123 is a safe repoint; land #123 first and it is an incident. No id consumed; count **76**, next free **#125** (reserved: #107).) · 2026-07-26 (**Record repair + next-session queue. No units, no code.** **#97's held set was WRONG IN BOTH DIRECTIONS and is corrected:** it read `5/6/9/10/11/13/14` — omitting rule **7** and wrongly holding rule **14** — while the JOURNAL recorded the next unit as "rule 1 + rule 14 leg (b)". Rule 14 cannot be both held and next. Held set is now **5/6/7/9/10/11/13**, and the **scheduled next unit** is recorded in the same edit with its **pinned predicate** (module = an entry in ARCHITECTURE's Modules table, name col 1 / path col 3; edge = any import whose target is an enumerated module regardless of location; `config` the forcing case). The arithmetic now closes: 7 held + 2 scheduled + 4 implemented + rule 12 structural = 14, which the earlier list did not. **#125 filed — rule 4 reads ONE surface while its spec claims FOUR.** ADR-15 landed in CLAUDE §11 and the README index but NOT in ARCHITECTURE's Governing-ADRs roster or its `ADR-01…14` header span, and the checker reported `FINDINGS 0` throughout: `_adr_roster_docs` is `("CLAUDE.md",)` alone, so `ARCHITECTURE.md` and the README are never read by rule 4. Rule 3 already reads two docs, so the narrowness is rule 4's, not the harness's. **LESSONS 2026-07-26 turned on the checker** — "rule 4 passes" published without its real predicate ("CLAUDE.md agrees"). ARCHITECTURE reconciled in the same commit (roster + header span + `last_reviewed`), so the stale surface is repaired and the defect that hid it is filed, not conflated. **NEXT-SESSION QUEUE (note, not a section — #102 remains a scheduled build):** **1.** #27 sitting — operator; instrument ready; resumable in parts. **2.** #117 ADR-11 decision-1 ruling — operator+architect; resolves #113 either way. **3.** Contract-Version 1.1 arc = **#34 + #76 + #100**, versioned together, never alone. **4.** #88 and #38 — independently schedulable contract-doc items (#88 carries **no #92 binding** in its live text). **5.** #103 — architect-owed design commission, **not CC work**. Behind the axes, in order: **#124 → #123**, then **#105**, then **#119/#120**. **Hygiene does not preempt items 1–5** — record repair, checker work and teardown are the background lane, and none of them outranks an operator- or architect-owed decision. Count **76 → 77**; next free **#126** (reserved: #107).) · 2026-07-26 (**Session close — record only.** **#105's `#112` citation given a historical marker in place, not deleted:** #112 closed 2026-07-26 (four dangling commits made reachable from pushed `archive/cited-*` tags, `gc.auto` restored, merge `e311e55`), so the reference now resolves as history rather than as a live id — the rule-13 class, found by hand because rule 13 is one of the seven HELD stubs and nothing mechanical looks for it. **#126 filed** — four armed pre-commit validators (`canonical_freshness_gate`, `validate_docs_registry`, `validate_sealed_keys`, `validate_audit_casing`) emit **no output at all**, bare or with `--all`, so a clean run and a run that checked **zero items** are byte-identical; their exit 0 is silence, not a verdict. CLAUDE §10's vacuous-pass anti-pattern and LESSONS 2026-07-26 in one object. Id checked in **both** spaces: the hub holds its own #126, nothing here references it, same disposition as the #116/#117 overlap. **#121 evidence upgrade:** the config sweep found **exactly one** local-only value with an external writer — `branch.main.vscode-merge-base=origin/main`, written by the VS Code git integration — so one of the three named candidates for the unattributed checkout now has **demonstrated write access to this checkout**. Recorded as narrowing the candidate set, **not** as proof of the checkout event. **#103 carries a dated NOT-STARTED note:** architect-owed browser work, displaced by this window's record and checker arcs, owed rather than dropped. **FLEET INTAKE COMMISSIONED 2026-07-26 to `.dev-knowledge`** — this window's fleet-level defects assembled into ten commissions **(A–J)**: environment/test isolation · session attribution and the multi-writer primary · the external-denominator principle · position-dependent template content · gate reproducibility · the parallel-work protocol · review routing · id-space and decision-record hygiene · handoff tooling defects · operational gotchas. **The HUB owns the rulings**; ai-council's role is evidence, and its refs are **#121, #122, #123, #124, #125, #73, #97, #116, #118, ADR-15 and LESSONS 2026-07-26**. Recorded here so the commission does not survive only in a chat transcript. Count **77 → 78**; next free **#127** (reserved: #107).)
