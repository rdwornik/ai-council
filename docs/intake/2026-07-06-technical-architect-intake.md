# Technical Architect Intake — ai-council entry-point document

**Date:** 2026-07-06 · **Content authored by:** the functional architect (authoritative) · **Assembled + path/section-verified by:** Claude Code (Fable), against live `main` @ `42e72a2`, 2026-07-06 · **Status:** committed record

---

### 0. How to use this document

You are the technical architect. The functional design layer is COMPLETE; nothing beyond documents has been built. Your deliverables are: (a) ratification waves for the draft ADRs listed in §5 (respecting their gates), (b) a backlog derived from the functional-requirements register in §4 plus each lane doc's §7 "inputs forward". The path here was: big picture → witnessed evidence (fleet recon) → five parallel per-lane functional designs → back to big picture (this document). The operator (Rob, solo operator) and the functional architect have answered every question they intend to answer; the complete list of open items is §7 — nothing else is pending from them. Standing rule for every backlog item you write: **a necessary condition ("done when"), testable, per item — closure on the hard end-state, never "tests pass" — no exceptions.**

### 1. The system in one screen

`ai-council` is the ecosystem's multi-model deliberation engine: structured debate/research across an AI panel producing verdicts that become binding ADRs for all repos under `Dev/`. Mission pillar #1 (the operator's declared goal): **reduce cognitive bias — debates as objective as achievable**; the other pillars: model/API currency, mode-lane discipline (research·CLI·API·pick), an open invocation interface for foreign repos via CC + terminal, and decision/path governance. Ratified core: ADR-11 (two-lane invocation contract, machine-facing CONTRACT doc live with two honest Known-deviations = the unbuilt D2 parity fixes) and ADR-12 (backend engine: v1 CLI adapters = claude+codex under four witnessed safety invariants; grok post-OAuth; agy excluded by identity roulette; default-flip evidence-gated). Empirical ground truth: 2026-07-05 live probes witnessed the whole fleet and 9/9 provider liveness; ~138 identity-readable historical syntheses exist locally (openai 56 / claude 54 / gemini 21) — the historical "Gemini default" was often openai in practice, which is now the free A/B corpus for the baseline. Feature work is paused; the pause lifts when GOV-1 executes (the push half of O2 was satisfied 2026-07-06).

### 2. Reading Map (verified by CC against live `main`, 2026-07-06)

| Document | Path | What it answers |
|---|---|---|
| Ratified decisions + index | `docs/decisions/ADR-01..12`, `docs/decisions/README.md` | What is settled; ADR-13 number reserved by the crux-resolver draft inside the 2026-07-04 audit (its §3, `DRAFT ADR-13-bounded-crux-check.md`) |
| Invocation contract (machine-facing) | `protocols/COUNCIL_INVOCATION_CONTRACT.md` | Lanes (§1), flags (§2), exit codes (§4), artifacts (§5), failure semantics (§6), Known deviations (§7 — exactly two, both D2 parity fixes), caller walkthrough (§8) |
| Question authoring + synthesis quality | `protocols/COUNCIL_QUESTION_GUIDE.md`, `protocols/SYNTHESIS_QUALITY_RUBRIC.md` | Brief discipline incl. research-mode recognition; the 5-criterion scoring instrument |
| Architecture audit (decisions D1–D14) | `docs/audits/2026-07-04-fable-architecture-audit.md` | The whole-system decision base (§4 tagged decision list D1–D14) + the reserved ADR-13 draft (§3) |
| Fleet recon + liveness + process specs P1–P4 + forks F1–F12 | `docs/audits/2026-07-05-fleet-recon-liveness-and-process-design.md` | Witnessed fleet matrix (§2), key/provider liveness (§3), the four process designs (§6 P1–P4), consolidated fork list (§8 F1–F12) |
| Functional design — L-EPI | `docs/intake/2026-07-06-lane-epi-functional-design.md` | Bias registry, falsifiers, EPI-1 archaeology protocol, gated #18/#19/#9 shapes, synthesis namespace |
| Functional design — L-CLI | `docs/intake/2026-07-06-lane-cli-functional-design.md` | Seat-equivalence contract, parity methodology, quota policy, seats[] namespace |
| Functional design — L-DOC | `docs/intake/2026-07-06-lane-doc-functional-design.md` | Doctor semantics/UX, pin-upgrade lifecycle, secrets rule |
| Functional design — L-INT | `docs/intake/2026-07-06-lane-int-functional-design.md` | Caller journey + failure paths, verdict package, versioning stance |
| Functional design — L-GOV | `docs/intake/2026-07-06-lane-gov-functional-design.md` | Enforcement inventory, ADR lifecycle, currency pass, hub-carrier requirements |
| Functional requirements master (navigation + register) | `docs/audits/2026-07-06-functional-requirements-master.md` | `[pending: file absent from live main — never committed; THIS intake supersedes it as the navigation layer. §4 below is the register of record.]` |
| Narrative + task record | `JOURNAL.md`, `LESSONS.md`, `BACKLOG.md` | History; the 6-epic story map (Epics A–F) your backlog extends |
| Historical audit stream (context only, added by CC) | `docs/audits/` 2026-05-09 … 2026-07-02 entries + `docs/audits/archive/`, per `docs/audits/README.md` | Pre-design-layer audits (Codex reviews, governance sweeps, the 2026-06-16 architecture-vs-research audit, the 2026-07-02 methodology-adoption audit) — background; superseded where they overlap the 2026-07-04/05 documents above |

**The one key to all five lane docs — uniform structure:** §1 charter & verified current state · §2 functional target state · §3 design answers (Q1–Q7 in L-CLI/L-EPI/L-INT; L-DOC has Q1–Q6, L-GOV Q1–Q5) · §4 draft ADRs · §5 refined forks · §6 operator questions · §7 requirements forward (your primary input).

**Topic Router (CC-verified against the live section headings):** metrics `seats[]` → L-CLI §3(Q5); metrics `synthesis` → L-EPI §3(Q7); seat-equivalence I1–I6 → L-CLI §3(Q1)/§4(DRAFT-CLI-1); parity methodology → L-CLI §3(Q2); quota policy → L-CLI §3(Q4)/§4(DRAFT-CLI-2); doctor semantics/UX → L-DOC §3(Q1–Q2)/§4(DRAFT-DOC-1); pin-upgrade lifecycle → L-DOC §3(Q4)/§4(DRAFT-DOC-2); secrets rule → L-DOC §3(Q6)/§4(DRAFT-DOC-3); caller journey → L-INT §3(Q1); verdict package → L-INT §3(Q2)/§4(DRAFT-INT-1); contract versioning → L-INT §3(Q7)/§4(DRAFT-INT-2); bias registry + falsifiers → L-EPI §3(Q1–Q2); EPI-1 archaeology → L-EPI §3(Q3); #18/#19/#9 shapes → L-EPI §3(Q4–Q6)/§4(DRAFT-EPI-3–5); ADR lifecycle → L-GOV §3(Q2)/§4(DRAFT-GOV-1); currency pass + watches → L-GOV §3(Q4–Q5)/§4(DRAFT-GOV-2); enforcement inventory → L-GOV §3(Q1); hub-carrier requirements → L-GOV §3(Q3) (full text in its §7).

### 3. Cross-lane seam contracts (canonical record — design against, never redesign)

1. **Metrics sidecar** — namespaced: `seats[]` owned by L-CLI; `synthesis` owned by L-EPI; neither extends the other's. Build rule: first lane to land defines the extension mechanism; never build both concurrently.
2. **Doctor** — owned by L-DOC; L-INT consumes it only as optional Lane-A pre-flight; L-CLI contributes exactly one requirement (identity-channel re-probe after CLI version change).
3. **Epic B gate** — owned by L-EPI: the EPI-1 report + operator ruling is the single event un-gating #18, #19, #9, D12/D13 and the v2 crux-resolver ranking (ADR-13).
4. **Parity evidence** — owned by L-CLI: only CLI-4 results ratify the ADR-12 §5 flip.
5. **Enforcement** — owned by L-GOV; hub-carrier work is a hub-level arc (this repo supplies requirements only).

### 4. Functional-requirements register (one line + pointer; canon lives in the lane docs — Evidence: lines added by CC)

**Interface (L-INT R1–R8):** R1 D2 parity fixes precede any real Lane-A commission · R2 verdict package per DRAFT-INT-1 (transcript-free caller) · R3 alarms/degradations persisted to artifacts, never console-only · R4 fail-loud, never bare exit 0 on failure · R5 duration envelope per mode in CONTRACT (caller kill budgets; --deep = hour-class) · R6 every contract addition states its Lane-B disposition · R7 version line + package version echo · R8 the CONTRACT failure-semantics table is the caller's surface.
**Evidence:** L-INT §7 (R1–R8 enumerated verbatim, each with rationale); §2 (the failure-semantics/timeout table R5/R8 reference); §3(Q2)/§4(DRAFT-INT-1) for R2; §3(Q7)/§4(DRAFT-INT-2) for R7; `protocols/COUNCIL_INVOCATION_CONTRACT.md` §6 (failure semantics) and §7 (the two Known deviations R1 closes).

**CLI backend (L-CLI):** seat-equivalence invariants I1–I6 (identity witnessed · scratch-cwd isolation · prompt parity · output normalization · anonymization unaffected · fallback semantics) · PRE-ADMISSION identity gate — an unverified seat never enters a round · fallback = visible YELLOW same-seat API retry, exit codes unchanged · quota defaults asymmetric-static: codex default-CLI in standard, claude per-run opt-in, grok excluded until OAuth · parity run n=12 stratified, sealed-key blind, non-inferiority 1/12, kill-condition on the faithfulness class · harness tax permanently segmentable in metrics · `seats[]` field spec with fallback-cause vocabulary = `classify_error` extension.
**Evidence:** L-CLI §3(Q1) (I1–I6 table with per-invariant observable checks; I6 names the failure-equivalence contract); §2.3 (pre-admission discard + "contract breach cannot happen by design" rows); §3(Q3) ("visible degradation, same-seat API retry, pre-admission gating — never silent, never retroactive"); §3(Q4) (quota policy + recommendation); §3(Q2) (n=12 blinded pairs, 1/12 margin, zero margin on rubric items 2 and 4); §3(Q6) (harness-contamination stance); §3(Q5) (`seats[]` fields incl. `fallback_events[]` cause vocabulary, `identity_readable`, `cli.version`); §4(DRAFT-CLI-1/2).

**Liveness (L-DOC):** `council doctor` subcommand; verdicts universally ADVISORY (the run-time health gate stays the only blocker) · doctor exit codes mirror verdict · stale-verdict rule (age > threshold reads STALE, not a color) · secrets rule: empty-string-is-absent-LOUDLY (closes the `cli.py:388` hazard) · pin-upgrade lifecycle: surface → evidence proportionate to epistemic load → operator decides → `chore(config)` commit, ADR only on channel change · next doctor confirms · zero-spend default, `--smoke` explicit · rolling retention + `doctor-latest.json`.
**Evidence:** L-DOC §3(Q1) (one-screen table + machine record); §3(Q2) ("the doctor is universally advisory; enforcement lives in the two places that already have authority"; exit codes 0=GREEN/3=YELLOW/1=RED in ADR-08 spirit); §3(Q3) (7-day STALE-VERDICT rule; retention 90 days / last 15 runs; `output/health/doctor-<ts>.json` + `doctor-latest.json`); §3(Q4) (five-step pin lifecycle incl. rubric-scored paired mini-run and `held(<date>, <reason>)`); §3(Q5) (zero-spend default, `--smoke`, version-change re-probe = the L-CLI seam requirement); §3(Q6) (treat-empty-as-absent-loudly, the recommended third stance); §1 (live confirmation of the `cli.py:388`/`:389` `override=False` hazard); §4(DRAFT-DOC-1/2/3).

**Epistemic (L-EPI):** 8-bias registry B1–B8 with two rules — no defense without a falsifier, no orphan bias · falsifiers M1–M8 · EPI-1 archaeology protocol (full local corpus + hub dedupe; ranked identity sources; blind scoring; min n≥10 per segment; single-recommendation report) — executable NOW, zero code · `synthesis` namespace (intended/actual author, intent_match) — record-and-alarm, never block · visible author line REQUIRED (F10 closed on archaeological evidence) · gated shapes: #18 conjunctive cruxhood + mechanical adjudication + fail-open; #19 triggers T1–T3, advisory-loud, never blocking; #9 checklist gate as pure function PASS|WARN|FAIL, dual placement, WARN-only initially.
**Evidence:** L-EPI §3(Q1) (B1–B8 registry table, existing-defense vs designed-defense vs residual-risk columns); §3(Q2) (M1–M8, each with measure + falsification condition); §3(Q3) (archaeology protocol; §1 gives the corpus counts and the n=10/n=21 gemini-segment arithmetic); §3(Q7) (`synthesis` fields incl. `intent_source`/`intent_match`; "record-and-alarm, never block"; visible "Synthesis (by X)" line normatively required); §5 (F10 "evidence is in; recommend closing for the visible line"); §3(Q4–Q6) + §4(DRAFT-EPI-3/4/5) (the three gated shapes, incl. T1–T3 triggers and the `{PASS|WARN|FAIL, findings[…]}` verdict schema); the ADR-13 shape it sharpens: `docs/audits/2026-07-04-fable-architecture-audit.md` §3.

**Governance (L-GOV):** ADR lifecycle Proposed/Accepted/Revised(dated append-only)/Superseded; status in exactly two synced places; ADR-09/10 → Accepted at GOV-1 · currency pass: triggers T1 post-ratification-merge + T2 30-day; close-out = push main; re-stamp-without-re-read forbidden · watch protocol: n=2 promotes (n=1 for safety class) · hub-carrier consumer requirements R1–R7 (input to the hub arc only) *[CC verification note: the live doc enumerates R1–**R8** — R8 (push-cadence surface) was added from 2026-07-06 session evidence after this register line was authored]*.
**Evidence:** L-GOV §3(Q2)/§4(DRAFT-GOV-1) (four states incl. `Proposed` expiry; "status lives in exactly two places — file header and `docs/decisions/README.md` index row — both change in the same commit"; ADR-09/10 flip disposition); §3(Q4)/§4(DRAFT-GOV-2) (triggers T1/T2/T3; fixed checklist; "pass close-out = push `main`"; "a re-stamp without a re-read is a protocol violation"); §3(Q5)/§4 (watch schema, n=2 promotion, n=1 safety class, seed watches W1–W3); §3(Q3) + §7 (hub-carrier R1–R8); §3(Q1) (enforcement inventory table with witnessed-value column).

### 5. Decision register (statuses CC-verified live 2026-07-06; file headers ↔ README index confirmed in sync)

**Ratified:** ADR-01..12. Live statuses: ADR-01/02/06 `Revised` (dated: 2026-04-30 / 2026-05-11 / 2026-05-11) · ADR-03/04/05/08 `Accepted` · ADR-07 `Superseded by ADR-43` · ADR-09/10 `Proposed` — these two (and only these two) flip to `Accepted` at GOV-1 execution per DRAFT-GOV-1 · ADR-11/12 `Accepted` (ratified 2026-07-05; ADR-12 §5 default-flip clause remains evidence-gated). *[CC correction: the seeded register read "01–10 currently Proposed pending the GOV-1 convention flip" — live files show only 09/10 Proposed; the GOV-1 flip applies to exactly those two.]*
**Evidence:** `docs/decisions/ADR-0*.md`/`ADR-1*.md` `**Status:**` header lines + `docs/decisions/README.md` index table (verified consistent pairwise).

**Draft ADRs — actionable now (11):** EPI-1 registry, EPI-2 author-identity, CLI-1 seat-equivalence, CLI-2 quota policy, DOC-1 doctor semantics, DOC-2 pin lifecycle, DOC-3 secrets rule, INT-1 verdict package, INT-2 versioning, GOV-1 lifecycle, GOV-2 currency+watch.
**Evidence:** each lane doc's §4, live status lines verified: DRAFT-EPI-1 `DRAFT` · DRAFT-EPI-2 `DRAFT (actionable-now class)` · DRAFT-CLI-1 `DRAFT` · DRAFT-CLI-2 `DRAFT` · DRAFT-DOC-1/2/3 `DRAFT` · DRAFT-INT-1 `DRAFT` (baseline-INDEPENDENT) · DRAFT-INT-2 `DRAFT` · DRAFT-GOV-1 `DRAFT` (mini-ADR) · DRAFT-GOV-2 `DRAFT`. Count verified: 11 actionable + 5 gated = 16 draft ADRs total across the five lane docs.

**Draft ADRs — gated (5):** EPI-3 (#18), EPI-4 (#19), EPI-5 (#9) — live status `DRAFT-GATED(Epic B #2 ruling)`; CLI-3 flip — live status `DRAFT-GATED(CLI-4 parity-run results)`; CLI-4 v2-resolver annotation — live status `DRAFT-GATED(EPI-2 ruling)`, glossed in the lane doc as "(Epic B baseline)", i.e. the Epic B ruling per seam contract §3.3. Reserved number: ADR-13.
**Evidence:** L-EPI §4 (DRAFT-EPI-3/4/5 status lines); L-CLI §4 (DRAFT-CLI-3/4 status lines) and §3(Q7); the reserved ADR-13 draft: `docs/audits/2026-07-04-fable-architecture-audit.md` §3 (`DRAFT ADR-13-bounded-crux-check.md — baseline-gated; do not ratify before Epic B`), reconfirmed untouched by `docs/audits/2026-07-05-fleet-recon-liveness-and-process-design.md` §7 (which adds the resolver-candidate ranking codex > claude > grok). *[CC disambiguation note: the label "CLI-4" is overloaded — L-CLI §3(Q2) uses "CLI-4" for the parity-run process itself (the gate on DRAFT-CLI-3), while DRAFT-CLI-4 is the separate v2-resolver annotation draft. Read gates accordingly.]*

**Operator rulings register:** 15 recommendations issued by the functional architect 2026-07-06 (spanning: Epic B comparative scoring scope, #19 default-on-post-calibration, blind-scoring authority split, F10 visible-author close, corpus scope, quota asymmetry, parity parameters, secrets rule, verdict-package variant, degradation-record scope, pause disposition O2, lifecycle ratification, watch location, push rule). Status: **RECOMMENDED — pending operator acceptance**; the consolidation session records them verbatim (full-text quotes from the lane docs' §6 for the three items whose summaries were garbled) and flips them to RULED. Until then, ruling-dependent items inherit pending status.
**Evidence:** the five lane docs' §6 ("Questions for the operator") — the recommendations' source text of record; not restated here per the consolidation-session boundary.

### 6. Operator ground truth (answers you cannot find in the repo)

The mission ordering is real: bias reduction outranks cost optimization — CLI lanes are economics, never epistemics (important + research profiles stay all-API by design). Cost posture: API dollars are not a constraint; the operator's own Claude Code subscription quota IS — hence claude-seat CLI is per-run opt-in. All five CLIs are installed and were operator-tested on this machine; grok subscription OAuth is deliberately not yet configured (F3, operator's 5-minute action). Standing rule: every session that lands commits ends with a JOURNAL entry naming a session SHA; push happens at close-out merges. The historical majority-openai synthesis record is treated as an asset (free A/B corpus), not an embarrassment.

### 7. Open items (the complete list — nothing else is pending)

1. The 15-item rulings register (§5) — operator acceptance → consolidation session records them, executes GOV-1, ratifies the lifecycle ADR → **feature-work pause lifts** (O2's push half done 2026-07-06).
2. EPI-1 execution (manual protocol, zero code, pause-independent) → operator's comparative ruling = the Epic B event → un-gates the five gated drafts.
3. F3: operator runs `grok login` + one re-probe → grok enters the cost lane.
4. Hub-side: D14 stale pointers + the enforcement-mesh carrier (hub arc; this repo's requirements are in L-GOV §3(Q3), full text in L-GOV §7).

### 8. Suggested derivation order (dependency, not calendar)

Wave A (post-rulings): consolidation + GOV-1 → pause lifts. Wave B (mission, starts immediately): EPI-1 archaeology → Epic B ruling. Wave C (build, post-lift): D2 parity (R1) ∥ doctor v1 ∥ EPI-2 identity+metrics (serialize with CLI `seats[]` per seam 1) → CliProvider v1 → CLI-4 parity run → flip decision. Wave D (gated): #18/#19/#9 + v2 resolver after the Epic B ruling. Ratify the actionable-now draft ADRs in waves that anchor each build before it starts. Every backlog item: testable done-when, closure on the hard end-state.

---

## Appendix — Assembly verification record (CC, 2026-07-06)

Everything above the rule is the functional architect's content, embedded verbatim except where marked *[CC …]*. Verification deltas found against live `main`:

1. **Reading Map:** `2026-07-06-functional-requirements-master.md` absent from `main` (never committed) → marked `[pending]`; §4 of this intake is the register of record. One row added (historical audit stream). All other paths, dates, and filenames verified existing.
2. **Topic Router corrections:** `seats[]` pointer tightened from bare "L-CLI §3" to §3(Q5); doctor semantics from "§3/§4" to §3(Q1–Q2)/§4(DRAFT-DOC-1); secrets rule gained §3(Q6); verdict package gained §3(Q2); contract versioning gained §3(Q7); ADR lifecycle gained §3(Q2); currency pass + watches gained §3(Q4–Q5); #18/#19/#9 §4 pointer named DRAFT-EPI-3–5; seat-equivalence and quota §4 pointers named DRAFT-CLI-1/2. All other entries confirmed correct as authored.
3. **Lane-doc structure key:** "§3 design answers (Q1–Q7)" holds for L-CLI/L-EPI/L-INT only; L-DOC has Q1–Q6 and L-GOV Q1–Q5 — corrected in place.
4. **Status corrections (§5):** ADR-01..10 are not uniformly Proposed — only ADR-09/10 are (01/02/06 Revised, 03/04/05/08 Accepted, 07 Superseded by ADR-43); header ↔ index sync confirmed. CLI-4 v2-resolver gate recorded verbatim as `DRAFT-GATED(EPI-2 ruling)` with its "(Epic B baseline)" gloss; the CLI-4 label collision (parity run vs resolver draft) flagged.
5. **Count correction (§4 Governance):** hub-carrier consumer requirements are R1–R8 live (R8 push-cadence surface), not R1–R7.
6. Verified consistent as authored: 16 draft ADRs (5 EPI, 4 CLI, 3 DOC, 2 INT, 2 GOV); two CONTRACT Known-deviations; I1–I6 / B1–B8 / M1–M8 / R1–R8 (INT) / T1–T3 (#19) enumerations; the 6-epic BACKLOG story map (Epics A–F); ADR-13 reservation.
7. **2026-07-16 (CC):** Reading-Map lane-doc paths updated `docs/audits/` → `docs/intake/` — the five lane FDs were relocated at `b849653` (census f.6); content untouched.
