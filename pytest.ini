[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests as integration tests (require real API keys)
    envcheck: marks tests that verify environment setup (API keys present)
