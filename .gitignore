# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.ruff_cache/
.pytest_cache/
.mypy_cache/
.hypothesis/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/*
!.vscode/settings.json
.idea/

# OS
desktop.ini
Thumbs.db

# Environment
.env

# Claude
.claude/settings.local.json
# Native parallel-session worktrees live in-repo here (ADR-61/#107); ignore so they don't
# show as untracked AND so gitignore-respecting walkers (ruff) don't descend into the full
# checkout. pytest is already safe via its default `.*` norecursedirs (dot-dir skip).
.claude/worktrees/

# Project-specific
output/
smoke-output/
.history/
council_inbox/archive/
council_inbox/*.md

# Data
*.db

# Ephemeral Tier-1 closure output
logs/

# >>> enforcement-mesh (ADR-85 /override) >>>
logs/.session-override-token
logs/OVERRIDES.md
!.claude/commands/
!.claude/commands/override.md
# <<< enforcement-mesh <<<

# EPI-1 archaeology workspace (#24) — governance home per ADR-60 (docs/audits), but gitignored
# WHILE the operator's blind scoring is active: preserves the blind seal + audit immutability
# (CLAUDE.md §5.3). Un-ignored + committed as the finalized evidence bundle at the Beat-1 close
# (once scoring is done and the #24 report records the un-blinding, the seal expires).
docs/audits/2026-07-17-epi1-archaeology/
docs/audits/2026-07-17-epi1-archaeology-KEY-SEALED.json
docs/audits/2026-07-17-epi1-archaeology-SECOND-OPINION-judge.md

# CLI-4 parity (#27) sealed backend key — local only, opened after scoring
docs/audits/2026-07-18-cli4-parity/SEALED-KEY.json
